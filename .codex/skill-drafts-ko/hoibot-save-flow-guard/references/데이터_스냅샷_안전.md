# 데이터 스냅샷 안전

`data/*.json` 파일은 production-like snapshot이다.

## 규칙

- original snapshot을 덮어쓰지 않는다.
- mutation script를 snapshot에 직접 실행하지 않는다.
- parsing 또는 mutation test는 copy를 사용한다.
- 가능하면 read-only parsing으로 JSON을 검증한다.

## 검증

JSON parse check는 파일을 쓰지 않는 read-only script를 선호한다.
