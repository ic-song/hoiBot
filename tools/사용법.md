# 호이봇 코드 작업 안내

이 문서는 Git 명령어를 직접 입력하지 않고 `tools/` 배치 파일로 코드 작업과 운영 반영을 진행하는 방법을 정리합니다.

---

## 1. 기본 작업 흐름

```text
01_새작업시작.bat 실행
↓
main.js 또는 Info.js 수정
↓
파일 저장
↓
02_수정내용올리기.bat 실행
↓
feature/prod 운영 반영 확인
```

---

## 2. 운영 반영

`03_main_운영반영.bat`와 `04_info_운영반영.bat`는 최신 `feature/prod`를 받은 뒤 LD플레이어 운영 봇에 파일을 업로드합니다.

두 스크립트 모두 `data/hoiBotChangeLog.json`을 함께 `/storage/emulated/0/호이랜드/hoiBotChangeLog.json`에 업로드합니다.

---

## 3. Codex Skills 설치/업데이트

작업 환경이 바뀌었거나 새 PC/새 Codex 환경에서 작업할 때 실행합니다.

```text
05_skills_설치.bat 실행
```

복사 대상:

```text
저장소 원본: .codex/skills/
로컬 사용:  %USERPROFILE%\.codex\skills\
한국어 검토본: .codex/skill-drafts-ko/
```

---

## 4. 배치파일 목록

| 파일 | 용도 |
| --- | --- |
| `01_새작업시작.bat` | 새 코드 작업을 시작할 때 사용합니다. |
| `02_수정내용올리기.bat` | 수정한 내용을 저장소에 올리고 `feature/prod`에 반영합니다. |
| `03_main_운영반영.bat` | 최신 `feature/prod`의 `main.js`와 `hoiBotChangeLog.json`을 LD플레이어 운영 봇에 반영합니다. |
| `04_info_운영반영.bat` | 최신 `feature/prod`의 `Info.js`와 `hoiBotChangeLog.json`을 LD플레이어 운영 봇에 반영합니다. |
| `05_skills_설치.bat` | 저장소의 Codex skills 원본을 로컬 Codex 설정 폴더로 설치/업데이트합니다. |
| `99_주의_망했을때_처음으로.bat` | 작업 상태를 되돌려야 할 때 주의해서 사용합니다. |
