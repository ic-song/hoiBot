import assert from "node:assert/strict";
import { loadConfig } from "../src/config.js";
import { createDatabaseClient,type DatabaseClient,type DatabaseTransaction } from "../src/database.js";
import { CommandDispatcher,MariaCommandDispatchRepository } from "../src/dispatch/command-dispatcher.js";
import { DataStatusService } from "../src/admin/data-status-service.js";
const config=loadConfig();if(!config.database.enabled)throw new Error("DATABASE_ENABLED must be true.");if(!/^hoibot_data_status(?:_[a-z0-9_]+)?$/i.test(config.database.name))throw new Error(`Synthetic data-status probe blocked: ${config.database.name}`);const db=createDatabaseClient(config.database),restart=process.argv.includes("--verify-restart"),user="synthetic-data-status-admin",room="synthetic-data-status-room",prefix=process.env.DATA_STATUS_PROBE_EVENT_ID??"data-status-g7-20260828-r1";
async function count(sql:string,v:readonly unknown[]=[]){return BigInt((await db.query<Array<{value:bigint|string}>>(sql,v))[0]?.value??0);}async function event(id:string,external=user){await db.execute("INSERT INTO event_inbox(event_id,provider_event_id,external_channel_id,external_user_id,event_kind,direction,payload_hash,processing_status,received_at) VALUES (?,?,?,?,'message','incoming',REPEAT('c',64),'processed',UTC_TIMESTAMP(3)) ON DUPLICATE KEY UPDATE processing_status=VALUES(processing_status)",[id,id,room,external]);}
try{const service=new DataStatusService(db);if(!restart){const op=988700006;await db.execute("INSERT INTO admin_operators(id,login_id,display_name,password_hash,status) VALUES (?,'synthetic-data-status','합성 데이터상태 관리자','synthetic','active') ON DUPLICATE KEY UPDATE status='active'",[op]);await db.execute("INSERT INTO admin_operator_roles(operator_id,role_id) SELECT ?,id FROM admin_roles WHERE code='super_admin' ON DUPLICATE KEY UPDATE role_id=VALUES(role_id)",[op]);await db.execute("INSERT INTO external_identities(provider_code,external_user_id,status) VALUES ('kakao',?,'linked') ON DUPLICATE KEY UPDATE status='linked'",[user]);await db.execute("INSERT INTO admin_operator_external_identities(operator_id,external_identity_id) SELECT ?,id FROM external_identities WHERE provider_code='kakao' AND external_user_id=? ON DUPLICATE KEY UPDATE operator_id=VALUES(operator_id)",[op,user]);await seed(db,"prod","synthetic-rev-1");const shadowId=`${prefix}-shadow`;await event(shadowId);const shadow=await new CommandDispatcher(new MariaCommandDispatchRepository(db),{enabled:true,allowAllCanaries:true,canaryUserIds:new Set()}).resolve({eventId:shadowId,message:"/데이터상태",userId:user,hasTrustedDisplayName:true});assert.deepEqual([shadow.route,shadow.handlerKey],["SHADOW","admin_data_status"]);assert.equal(await count("SELECT COUNT(*) value FROM operations WHERE idempotency_key=?",[shadowId]),0n);const readId=`${prefix}-read`;await event(readId);const result=await service.read({eventId:readId,externalUserId:user,destinationId:room,environment:"prod"});assert.ok(result!==null);assert.equal(result.objects.length,12);assert.match(result.data,/revision: synthetic-rev-1/);assert.match(result.data,/1차: 손상/);assert.match(result.data,/2차: 누락/);assert.deepEqual(await service.read({eventId:readId,externalUserId:user,destinationId:room,environment:"prod"}),result);const denied=`${prefix}-denied`;await event(denied,`${user}-denied`);assert.equal(await service.read({eventId:denied,externalUserId:`${user}-denied`,destinationId:room,environment:"prod"}),null);const rollback=`${prefix}-rollback`;await event(rollback);await assert.rejects(()=>new DataStatusService(failAudit(db)).read({eventId:rollback,externalUserId:user,destinationId:room,environment:"prod"}),/synthetic data status audit failure/);assert.equal(await count("SELECT COUNT(*) value FROM operations WHERE idempotency_key=?",[rollback]),0n);const effects={operations:await count("SELECT COUNT(*) value FROM operations WHERE idempotency_scope='backup_status.read'"),executions:await count("SELECT COUNT(*) value FROM command_executions WHERE command_code='ADMIN_DATA_STATUS'"),outboxes:await count("SELECT COUNT(*) value FROM outbox_messages o JOIN operations p ON p.id=o.operation_id WHERE p.idempotency_scope='backup_status.read'"),audits:await count("SELECT COUNT(*) value FROM command_audit a JOIN operations p ON p.id=a.operation_id WHERE p.idempotency_scope='backup_status.read'")};assert.deepEqual(effects,{operations:1n,executions:1n,outboxes:1n,audits:1n});console.log(JSON.stringify({mode:"probe",migrationCount:270,revision:"synthetic-rev-1",objects:12,scenarios:["shadow","permission","healthy","missing","damaged","revision-pin","replay","rollback"],effects,domainMutation:0,operationalDataTouched:false},(_k,v)=>typeof v==="bigint"?Number(v):v));}else{const before=await count("SELECT COUNT(*) value FROM operations WHERE idempotency_scope='backup_status.read'");const replay=await service.read({eventId:`${prefix}-read`,externalUserId:user,destinationId:room,environment:"prod"});assert.ok(replay!==null);assert.equal(replay.revisionKey,"synthetic-rev-1");assert.equal(await count("SELECT COUNT(*) value FROM operations WHERE idempotency_scope='backup_status.read'"),before);console.log(JSON.stringify({mode:"verify-restart",revision:replay.revisionKey,objects:replay.objects.length,replayPreserved:true,operations:before,additionalMutation:false,operationalDataTouched:false},(_k,v)=>typeof v==="bigint"?Number(v):v));}}finally{await db.close();}
async function seed(db:DatabaseClient,env:string,revision:string){const generation=await db.execute("INSERT INTO backup_generations(environment_code,revision_key,generation_status,created_at,completed_at) VALUES (?,?,'complete',UTC_TIMESTAMP(3),UTC_TIMESTAMP(3))",[env,revision]);for(const target of ["member","member_pet","petSkillData","petHomeActivityData"])for(const slot of ["original","backup1","backup2"]){const exists=!(target==="petHomeActivityData"&&slot==="backup2"),valid=!(target==="petSkillData"&&slot==="backup1");const object=await db.execute("INSERT INTO backup_objects(generation_id,target_code,slot_code,object_exists,content_sha256,size_bytes,modified_at,storage_locator) VALUES (?,?,?,?,REPEAT('d',64),100,UTC_TIMESTAMP(3),'synthetic')",[generation.insertId,target,slot,exists]);await db.execute("INSERT INTO backup_health_checks(backup_object_id,valid_json,error_code,checked_at) VALUES (?,?,?,UTC_TIMESTAMP(3))",[object.insertId,valid,valid?null:"JSON_PARSE"]);}}
function failAudit(inner:DatabaseClient):DatabaseClient{return{ping:()=>inner.ping(),query:(sql,v)=>inner.query(sql,v),execute:(sql,v)=>inner.execute(sql,v),verifyRollback:()=>inner.verifyRollback(),close:async()=>undefined,withTransaction:<T>(work:(tx:DatabaseTransaction)=>Promise<T>)=>inner.withTransaction(tx=>work({query:(sql,v)=>tx.query(sql,v),execute:async(sql,v)=>{if(sql.includes("INSERT INTO command_audit"))throw new Error("synthetic data status audit failure");return tx.execute(sql,v);}}))};}
