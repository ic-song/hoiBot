import assert from "node:assert/strict";import{describe,it}from"node:test";import{isAuctionBidCandidate,normalizeAuctionBidDispatchMessage,parseAuctionBidCommand}from"../src/market/auction-bid-service.js";import{isFreeMarketLifecycleCandidate,isFreeMarketMutationDispatch,normalizeFreeMarketLifecycleDispatchMessage}from"../src/market/free-market-buy-service.js";
describe("auction bid command boundary",()=>{it("accepts exact usage and two positive integer arguments",()=>{assert.equal(isAuctionBidCandidate("/입찰"),true);assert.deepEqual(parseAuctionBidCommand("/입찰 7 3000000"),{listingNo:7n,amount:3000000n});assert.equal(normalizeAuctionBidDispatchMessage("/입찰 7 3000000"),"/입찰 [아이템번호] [포인트]");});it("rejects zero overflow decimal negative and suffix forms",()=>{for(const value of ["/입찰 0 1","/입찰 1 0","/입찰 18446744073709551616 1","/입찰 1 1.0","/입찰 1 -1","/입찰 1 2 안내"])assert.equal(parseAuctionBidCommand(value),null);});it("reuses the existing market dispatch boundary",()=>{assert.equal(isFreeMarketLifecycleCandidate("/입찰 1 2"),true);assert.equal(normalizeFreeMarketLifecycleDispatchMessage("/입찰 1 2"),"/입찰 [아이템번호] [포인트]");assert.equal(isFreeMarketMutationDispatch("/입찰 1 2","MODERN","store_auction_bid"),true);});});
