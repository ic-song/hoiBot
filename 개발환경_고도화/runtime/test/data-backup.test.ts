import assert from "node:assert/strict";import {describe,it} from "node:test";import {buildSourceRevisionKey,formatDataBackupResult,isDataBackupCommand,isTopLevelFileName} from "../src/admin/data-backup-service.js";
describe("admin data backup",()=>{it("accepts only the exact DEV command",()=>{assert.equal(isDataBackupCommand("dev/데이터백업"),true);for(const value of ["/데이터백업","dev/데이터백업 ","dev/데이터백업 1","/데이터백업2"])assert.equal(isDataBackupCommand(value),false);});it("keeps only top-level file names",()=>{assert.equal(isTopLevelFileName("member.json"),true);assert.equal(isTopLevelFileName(""),false);assert.equal(isTopLevelFileName("sub/member.json"),false);assert.equal(isTopLevelFileName("sub\\member.json"),false);});it("pins sorted source revisions and hashes",()=>{const a=buildSourceRevisionKey([{file_name:"a.json",content_sha256:"a".repeat(64),revision_version:1n},{file_name:"b.txt",content_sha256:"b".repeat(64),revision_version:2n}]);const b=buildSourceRevisionKey([{file_name:"a.json",content_sha256:"a".repeat(64),revision_version:1n},{file_name:"b.txt",content_sha256:"b".repeat(64),revision_version:2n}]);assert.equal(a,b);assert.match(a,/^sha256:[0-9a-f]{64}$/);});it("formats copied and empty lists",()=>{assert.equal(formatDataBackupResult(["a.json","b.txt"]),"✅ DEV 데이터 백업 완료\n복사 파일: 2개\n- a.json\n- b.txt");assert.match(formatDataBackupResult([]),/복사할 운영 파일 없음/);});});
