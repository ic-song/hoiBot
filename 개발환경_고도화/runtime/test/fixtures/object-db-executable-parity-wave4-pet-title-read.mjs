import {createHash,randomUUID} from "node:crypto";import{readFileSync}from"node:fs";import{dirname,resolve}from"node:path";import{fileURLToPath,pathToFileURL}from"node:url";import{tsImport}from"tsx/esm/api";
const SOURCE={file:"개발환경_고도화/runtime/src/pet/pet-title-canonical-read-provider.ts",start:922,end:2211,sha256:"6794b0c2729a7c993382a1c553aecb7a399ef3b26a9c210274a02c5fe25fa2d3"},ID="sql-repository-9cfe67ded7b1e5b0",MODULE_EXECUTION_ID=randomUUID(),SCENARIOS=["READ_POSITIVE","NEGATIVE_GUARD","EXACT_OUTPUT","SOURCE_DOMAIN_DML_ZERO","RESTART_CONSISTENCY"],RESULT=[{instanceId:"ownpet01",displayName:"펫 타이틀✨",priceDigits:"100000000",acquiredAt:"2026-09-06 11:35:00",equipped:true}];
function assert(v,m){if(!v)throw new Error(m)}
export async function executeWave4PetTitleRead({consumerId,harnessCaseId,scenarioKind,fixturePayload,database}){let assertionCount=0;const check=(v,m)=>{assertionCount++;assert(v,m)},c=fixturePayload.cases.find(x=>x.caseId===harnessCaseId),consumer=c?.consumers.find(x=>x.consumerId===consumerId);check(consumerId===ID&&c?.executablePath==="PetTitleCanonicalReadProvider.listOwned"&&c.transactionPath==="DatabaseClient.query:READ_ONLY","case drift");check(JSON.stringify(c.requiredScenarios)===JSON.stringify(SCENARIOS)&&consumer?.sourceLocator.sha256===SOURCE.sha256,"fixture drift");const root=resolve(dirname(fileURLToPath(import.meta.url)),"../../../.."),path=resolve(root,SOURCE.file),source=readFileSync(path,"utf8").replace(/\r\n?/g,"\n"),span=source.slice(SOURCE.start,SOURCE.end);check(createHash("sha256").update(span).digest("hex")===SOURCE.sha256&&span.includes("async listOwned("),"source drift");const{PetTitleCanonicalReadProvider}=await tsImport(pathToFileURL(path).href,import.meta.url),provider=new PetTitleCanonicalReadProvider();if(scenarioKind==="NEGATIVE_GUARD"){let error=null;try{await provider.listOwned(database,consumer.negativeInput.playerId)}catch(e){error=e instanceof Error?e.message:String(e)}check(error==="OBJECT_IDENTITY_CANDIDATE_INVALID","guard drift");return{executedConsumerId:ID,executedCaseId:harnessCaseId,moduleExecutionId:MODULE_EXECUTION_ID,assertionCount,reply:error,result:JSON.stringify({error,queryCount:0})}}const result=await provider.listOwned(database,consumer.input.playerId);check(JSON.stringify(result)===JSON.stringify(RESULT),"result drift");const raw=JSON.stringify(result);return{executedConsumerId:ID,executedCaseId:harnessCaseId,moduleExecutionId:MODULE_EXECUTION_ID,assertionCount,reply:raw,result:raw}}
