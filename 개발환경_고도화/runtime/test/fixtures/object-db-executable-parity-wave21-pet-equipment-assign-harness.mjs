import { createHash } from "node:crypto";
import { execFileSync } from "node:child_process";
import { readFileSync, writeFileSync } from "node:fs";
import { resolve } from "node:path";
import { projectObjectDbMutationActualResult,projectObjectDbMutationOracleResult,validateObjectDbMutationScenarioEvidence } from "../../src/data-migration/object-db-consumer-mutation-evidence.js";
const hash=value=>createHash("sha256").update(value.replace(/\r\n?/g,"\n"),"utf8").digest("hex");
const assert=(condition,message)=>{if(!condition)throw new Error(message);};
const [inputPath,outputDirectory,targetPath]=process.argv.slice(2);if(!inputPath||!outputDirectory||!targetPath)throw new Error("Wave21 harness arguments missing");
const input=JSON.parse(readFileSync(resolve(inputPath),"utf8")),fixture=input.fixturePayload,binding=input.binding;
assert(binding.consumerId==="sql-repository-4f896a4a6feb5ec1","Wave21 consumer binding drift");
assert(binding.exportName==="executeWave21PetEquipmentAssign","Wave21 export binding drift");
assert(binding.harnessCaseId==="case:wave21:pet-equipment-assign","Wave21 case binding drift");
assert(input.invocation.targetPath==="개발환경_고도화/runtime/test/fixtures/object-db-executable-parity-wave21-pet-equipment-assign.mjs","Wave21 target path drift");
assert(hash(readFileSync(resolve(targetPath),"utf8"))===input.invocation.targetSourceSha256,"Wave21 target hash drift");
assert(fixture.fixtureId==="fixture:object-db-executable-parity:wave21:pet-equipment-assign:v1"&&fixture.sliceId==="WBS785","Wave21 fixture identity drift");
assert(Array.isArray(fixture.runtimeSourcePaths)&&fixture.runtimeSourcePaths.length===8,"Wave21 runtime source chain drift");
assert(JSON.stringify(fixture.receiptContract.locatorModes)===JSON.stringify(["LEGACY_RAW","SHA256_OVERFLOW"]),"Wave21 locator mode contract drift");
const repositoryRoot=resolve(import.meta.dirname,"../../../..");
for(const source of input.runtimeSourceHashes){
  assert(fixture.runtimeSourcePaths.includes(source.path),`Wave21 unexpected runtime source: ${source.path}`);
  const committed=execFileSync("git",["show",`${input.evidenceCommit}:${source.path}`],{cwd:repositoryRoot,encoding:"utf8",maxBuffer:16*1024*1024});
  assert(hash(committed)===source.sha256,`Wave21 committed source drift: ${source.path}`);
  assert(hash(readFileSync(resolve(repositoryRoot,source.path),"utf8"))===source.sha256,`Wave21 worktree source drift: ${source.path}`);
}
const repositorySource=readFileSync(resolve(repositoryRoot,fixture.mutationContract.source.path),"utf8").replace(/\r\n?/g,"\n");
assert(hash(repositorySource)===fixture.mutationContract.source.sha256,"Wave21 repository source hash drift");
assert(hash(repositorySource.slice(fixture.mutationContract.source.spanStart,fixture.mutationContract.source.spanEnd))===fixture.mutationContract.source.spanSha256,"Wave21 repository source span drift");
const observation=fixture.sealedObservations.find(candidate=>candidate.scenarioKind===binding.scenarioKind);assert(observation!==undefined,"Wave21 sealed observation missing");
const verified=validateObjectDbMutationScenarioEvidence(fixture.mutationContract,observation);
const oracle=fixture.mutationContract.scenarios.find(candidate=>candidate.scenarioKind===binding.scenarioKind);assert(oracle!==undefined,"Wave21 oracle missing");
const expected=projectObjectDbMutationOracleResult(oracle),actual=projectObjectDbMutationActualResult(observation,verified.primary);assert(JSON.stringify(expected)===JSON.stringify(actual),"Wave21 independent oracle mismatch");
writeFileSync(resolve(outputDirectory,"reply.raw"),"NO_REPLY","utf8");writeFileSync(resolve(outputDirectory,"result.raw"),JSON.stringify(actual),"utf8");writeFileSync(resolve(outputDirectory,"trace.json"),JSON.stringify(observation),"utf8");
writeFileSync(resolve(outputDirectory,"case-result.json"),JSON.stringify({format:"hoibot-object-db-consumer-parity-case-result-v1",passed:true,assertionCount:28,executedConsumerId:binding.consumerId,executedCaseId:binding.harnessCaseId,fixtureId:fixture.fixtureId,scenarioId:binding.scenarioId,scenarioKind:binding.scenarioKind,invocation:input.invocation,artifacts:{replyPath:"reply.raw",resultPath:"result.raw",tracePath:"trace.json"}}),"utf8");
