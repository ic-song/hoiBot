import assert from"node:assert/strict";import{describe,it}from"node:test";import{isCarrotBoardAddCandidate,normalizeCarrotBoardAddCommand,parseCarrotBoardAddBody}from"../src/market/carrot-board-add-service.js";
describe("carrot board add",()=>{it("accepts the exact command for usage output",()=>{assert.equal(isCarrotBoardAddCandidate("/당근등록"),true);assert.equal(normalizeCarrotBoardAddCommand("/당근등록"),"/당근등록");});it("accepts 1 through 49 trimmed characters",()=>{assert.equal(parseCarrotBoardAddBody("/당근등록 거래합니다"),"거래합니다");assert.equal(parseCarrotBoardAddBody(` /당근등록 ${"a".repeat(49)}`),null);assert.equal(parseCarrotBoardAddBody(`/당근등록 ${"a".repeat(49)}`),"a".repeat(49));});it("rejects 50 characters",()=>assert.equal(parseCarrotBoardAddBody(`/당근등록 ${"a".repeat(50)}`),null));it("rejects newlines and empty bodies",()=>{assert.equal(parseCarrotBoardAddBody("/당근등록   "),null);assert.equal(parseCarrotBoardAddBody("/당근등록 a\nb"),null);});it("blocks prefix collisions",()=>{assert.equal(isCarrotBoardAddCandidate("/당근등록됨"),false);assert.equal(isCarrotBoardAddCandidate("/당근등록2 x"),false);});});
