import assert from"node:assert/strict";import{after,before,describe,it}from"node:test";import{buildApp}from"../src/app.js";import{loadConfig}from"../src/config.js";import{createDatabaseClient,type DatabaseClient}from"../src/database.js";const enabled=process.env.DATABASE_INTEGRATION_ENABLED==="true",required=(n:string)=>process.env[n]??"integration-not-configured";
describe("home alert read SHADOW",{skip:!enabled},()=>{let db:DatabaseClient;const suffix=Date.now().toString(),external=`alert-shadow-${suffix}`,token="home-alert-shadow-token",room="990000000000622";before(async()=>{db=createDatabaseClient({enabled:true,host:required("DATABASE_HOST"),port:Number(required("DATABASE_PORT")),user:required("DATABASE_USER"),password:required("DATABASE_PASSWORD"),name:required("DATABASE_NAME"),connectionLimit:3,connectTimeoutMs:5000});const p=await db.execute("INSERT INTO players(status,version) VALUES('active',1)");await db.execute("INSERT INTO player_profiles(player_id,current_display_name,version) VALUES(?,'알림 SHADOW',1)",[p.insertId]);await db.execute("INSERT INTO external_identities(player_id,provider_code,external_user_id,display_name,status) VALUES(?,'kakao',?,'알림 SHADOW','linked')",[p.insertId,external]);await db.execute("UPDATE command_registry SET rollout_state='SHADOW' WHERE command_code='HOME_ACTIVITY_ALERT_READ'");});after(async()=>{if(db)try{await db.close();}catch{}});it("does not mutate",async()=>{const config=loadConfig({NODE_ENV:"test",IRIS_SHARED_TOKEN:token,USER_VERIFICATION_PEPPER:"home-alert-shadow-pepper",DATABASE_ENABLED:"true",DATABASE_HOST:required("DATABASE_HOST"),DATABASE_PORT:required("DATABASE_PORT"),DATABASE_USER:required("DATABASE_USER"),DATABASE_PASSWORD:required("DATABASE_PASSWORD"),DATABASE_NAME:required("DATABASE_NAME")});process.env.HOME_ACTIVITY_ALERT_READ_COMMAND_ENABLED="true";process.env.PARTIAL_COMMAND_DISPATCH_ENABLED="true";const app=buildApp(config,{database:db,inspectIrisChannel:async()=>({mode:"operational",channelClass:"open_group",reason:"allowed",evidence:{roomType:"OM",openLinkActive:true,openLinkExpired:false}}),sendIrisTextReply:async()=>{}});await app.inject({method:"POST",url:`/api/v1/integrations/iris/events?token=${token}`,payload:{msg:"/홈알림",room:"고도화펫테스트방",sender:"알림 SHADOW",json:{_id:`alert-shadow-event-${suffix}`,chat_id:room,user_id:external}}});assert.equal((await db.query<Array<{count:bigint}>>("SELECT COUNT(*) count FROM operations WHERE idempotency_scope='home.activity_alert_read' AND idempotency_key=?",["alert-shadow-event-"+suffix]))[0]!.count,0n);delete process.env.HOME_ACTIVITY_ALERT_READ_COMMAND_ENABLED;await app.close();});});
