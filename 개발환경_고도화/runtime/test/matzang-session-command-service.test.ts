import assert from"node:assert/strict";import{describe,it}from"node:test";import{isMatzangSessionCommand,parseMatzangSessionCommand}from"../src/battle/matzang-session-command-service.js";describe("matzang session command boundary",()=>{it("accepts only exact start and end commands",()=>{assert.equal(parseMatzangSessionCommand("/맞짱시작"),"start");assert.equal(parseMatzangSessionCommand("/맞짱종료"),"end");assert.equal(isMatzangSessionCommand("/맞짱시작 지금"),false);assert.equal(isMatzangSessionCommand("/맞짱종료1"),false);});});
