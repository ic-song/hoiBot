import assert from "node:assert/strict";import{createHash}from"node:crypto";import{after,before,describe,it}from"node:test";import{buildApp}from"../src/app.js";import{loadConfig}from"../src/config.js";import{createDatabaseClient,type DatabaseClient}from"../src/database.js";import{MemberCharacterCountService}from"../src/admin/member-character-count-service.js";
const enabled=process.env.DATABASE_INTEGRATION_ENABLED==="true",required=(name:string):string=>process.env[name]??"integration-not-configured";
describe("member character count MariaDB integration",{skip:!enabled},()=>{let database:DatabaseClient;const suffix=`${process.pid}-${Date.now()}`,token="member-character-count-token",roomId="990000000000451",externalUserId=`member-count-user-${suffix}`;let identityId=0n,shadowSourceEventId="";const rawJson=JSON.stringify({"호이😀":{point:7,bag:["토끼🐰"]}},null,2);
before(async()=>{database=createDatabaseClient({enabled:true,host:required("DATABASE_HOST"),port:Number(required("DATABASE_PORT")),user:required("DATABASE_USER"),password:required("DATABASE_PASSWORD"),name:required("DATABASE_NAME"),connectionLimit:5,connectTimeoutMs:5000});await database.execute("UPDATE command_registry SET rollout_state='ACTIVE',enabled=1 WHERE command_code='ADMIN_MEMBER_CHARACTER_COUNT'");await database.execute("INSERT INTO players(status,version) VALUES ('active',1)");const player=(await database.query<Array<{id:bigint}>>("SELECT id FROM players ORDER BY id DESC LIMIT 1"))[0]!;await database.execute("INSERT INTO player_profiles(player_id,current_display_name,version) VALUES (?,'멤버 글자수 회원',1)",[player.id]);await database.execute("INSERT INTO external_identities(player_id,provider_code,external_user_id,display_name,status) VALUES (?,'kakao',?,'멤버 글자수 회원','linked')",[player.id,externalUserId]);identityId=(await database.query<Array<{id:bigint}>>("SELECT id FROM external_identities WHERE provider_code='kakao' AND external_user_id=?",[externalUserId]))[0]!.id;await database.execute("INSERT INTO pet_member_storage_snapshots(snapshot_code,raw_json,content_sha256,utf16_character_count,source_version,captured_at) VALUES ('member',?,?,?,?,UTC_TIMESTAMP(3)) ON DUPLICATE KEY UPDATE raw_json=VALUES(raw_json),content_sha256=VALUES(content_sha256),utf16_character_count=VALUES(utf16_character_count),source_version=VALUES(source_version),captured_at=VALUES(captured_at)",[rawJson,createHash("sha256").update(rawJson).digest("hex"),rawJson.length,`synthetic-${suffix}`]);process.env.PARTIAL_COMMAND_DISPATCH_ENABLED="true";});
after(async()=>{delete process.env.PARTIAL_COMMAND_DISPATCH_ENABLED;if(database)try{await database.close();}catch(error){const code=typeof error==="object"&&error!==null&&"code"in error?(error as{code?:unknown}).code:undefined;if(code!=="ER_POOL_ALREADY_CLOSED")throw error;}});
const config=()=>loadConfig({NODE_ENV:"test",IRIS_SHARED_TOKEN:token,USER_VERIFICATION_PEPPER:"member-count-pepper",DATABASE_ENABLED:"true",DATABASE_HOST:required("DATABASE_HOST"),DATABASE_PORT:required("DATABASE_PORT"),DATABASE_USER:required("DATABASE_USER"),DATABASE_PASSWORD:required("DATABASE_PASSWORD"),DATABASE_NAME:required("DATABASE_NAME")});
it("queues the exact member count once for a repeated event",async()=>{const replies:string[]=[],app=buildApp(config(),{database,inspectIrisChannel:async()=>({mode:"operational",channelClass:"open_group",reason:"allowed",evidence:{roomType:"OM",openLinkActive:true,openLinkExpired:false}}),sendIrisTextReply:async reply=>{replies.push(reply.data);}}),eventId=`member-count-${Date.now()}`,send=()=>app.inject({method:"POST",url:`/api/v1/integrations/iris/events?token=${token}`,payload:{msg:"/멤버글자수",room:"고도화팻테스트방",sender:"멤버 글자수 회원",json:{_id:eventId,chat_id:roomId,user_id:externalUserId}}});assert.equal((await send()).statusCode,202);await send();assert.deepEqual(replies,[`총 글자 수 : ${rawJson.length.toString().replace(/\B(?=(\d{3})+(?!\d))/g,",")}`]);assert.equal(Number((await database.query<Array<{count:bigint}>>("SELECT COUNT(*) count FROM operations WHERE idempotency_key=? AND idempotency_scope LIKE 'admin.member_character_count:%'",[`iris:${eventId}`]))[0]!.count),1);});
it("keeps Shadow free of operations and outbox",async()=>{await database.execute("UPDATE command_registry SET rollout_state='SHADOW' WHERE command_code='ADMIN_MEMBER_CHARACTER_COUNT'");const app=buildApp(config(),{database,inspectIrisChannel:async()=>({mode:"operational",channelClass:"open_group",reason:"allowed",evidence:{roomType:"OM",openLinkActive:true,openLinkExpired:false}}),sendIrisTextReply:async()=>undefined}),eventId=`member-count-shadow-${Date.now()}`;shadowSourceEventId=`iris:${eventId}`;assert.equal((await app.inject({method:"POST",url:`/api/v1/integrations/iris/events?token=${token}`,payload:{msg:"/멤버글자수",room:"고도화팻테스트방",sender:"멤버 글자수 회원",json:{_id:eventId,chat_id:roomId,user_id:externalUserId}}})).statusCode,202);assert.equal(Number((await database.query<Array<{count:bigint}>>("SELECT COUNT(*) count FROM operations WHERE idempotency_key=?",[`iris:${eventId}`]))[0]!.count),0);assert.equal((await database.query<Array<{route:string}>>("SELECT route FROM command_routing_decisions WHERE event_id=?",[`iris:${eventId}`]))[0]!.route,"SHADOW");});
it("rolls back operation and outbox when audit fails",async()=>{await database.execute("CREATE TRIGGER fail_member_character_count_audit BEFORE INSERT ON command_audit FOR EACH ROW BEGIN IF NEW.action_code='member.character_count' THEN SIGNAL SQLSTATE '45000' SET MESSAGE_TEXT='forced member character count audit failure'; END IF; END");const eventId=`member-count-rollback-${Date.now()}`;try{await assert.rejects(()=>new MemberCharacterCountService(database).count({idempotencyKey:eventId,sourceEventId:shadowSourceEventId,destinationId:roomId,identityId:identityId.toString()}),/forced member character count audit failure/);}finally{await database.execute("DROP TRIGGER IF EXISTS fail_member_character_count_audit");}const state=(await database.query<Array<{operations:bigint;outboxes:bigint}>>("SELECT (SELECT COUNT(*) FROM operations WHERE idempotency_key=?) operations,(SELECT COUNT(*) FROM outbox_messages outbox JOIN operations operation ON operation.id=outbox.operation_id WHERE operation.idempotency_key=?) outboxes",[eventId,eventId]))[0]!;assert.deepEqual(state,{operations:0n,outboxes:0n});});
});
