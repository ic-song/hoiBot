import assert from"node:assert/strict";import{describe,it}from"node:test";import{formatCarrotBoardDeleteResult,isCarrotBoardDeleteCommand}from"../src/market/carrot-board-delete-service.js";describe("carrot board delete",()=>{it("accepts exact command only",()=>{assert.equal(isCarrotBoardDeleteCommand("/당근게시판삭제"),true);assert.equal(isCarrotBoardDeleteCommand("/당근게시판삭제 "),false);assert.equal(isCarrotBoardDeleteCommand("/당근게시판삭제 1"),false);});it("formats cleared count",()=>assert.equal(formatCarrotBoardDeleteResult(3),"✅ 당근게시판 게시글 3개를 삭제했습니다."));it("formats empty board",()=>assert.equal(formatCarrotBoardDeleteResult(0),"당근게시판에 삭제할 게시글이 없습니다."));});
