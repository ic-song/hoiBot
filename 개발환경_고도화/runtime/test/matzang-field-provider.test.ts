import assert from"node:assert/strict";import{describe,it}from"node:test";import{critChance,critMultiplier,resolveMatzangBattle,selectOpponent,typeBuff}from"../src/battle/matzang-field-provider.js";
describe("matzang field policy",()=>{it("preserves sky-land-sea advantage",()=>{assert.deepEqual(typeBuff("하늘","땅"),[1.3,1]);assert.deepEqual(typeBuff("땅","하늘"),[1,1.3]);assert.deepEqual(typeBuff("기타","땅"),[1,1]);});it("preserves capped critical chance and 301+ multiplier",()=>{assert.equal(critChance(100),.5);assert.equal(critChance(200),.8);assert.equal(critChance(300),.9);assert.equal(critChance(999),.9);assert.equal(critMultiplier(301),1.71);});it("selects stable opponent boundaries",()=>{assert.equal(selectOpponent(["a","b"],0),"a");assert.equal(selectOpponent(["a","b"],.999),"b");});it("lets defender win ties and applies outcome PT range",()=>{const p={playerId:"1",displayName:"a",base:100,petType:"하늘",upgrade:0};const q={playerId:"2",displayName:"b",base:130,petType:"땅",upgrade:0};const r=resolveMatzangBattle(p,q,{attackerCrit:.9,defenderCrit:.9,pt:.999},{min:5,max:7,reward:"25000000"});assert.equal(r.attackerWin,false);assert.equal(r.gainedPt,7);});});
