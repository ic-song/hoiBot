import { createHash,randomUUID } from "node:crypto";
import type { DatabaseClient,DatabaseTransaction } from "../database.js";
import { sortPendantBagEntries,type PendantBagEntry } from "./pendant-bag-service.js";
interface Operator{operator_id:bigint;} interface Row{instance_id:bigint;player_id:bigint;version:bigint;item_name:string;name_value:string|null;icon_value:string|null;grade_value:string|null;durability_value:string|null;max_durability_value:string|null;upgrade_value:string|null;} interface Target{row:Row;entry:PendantBagEntry;}
export interface PendantDeleteResult{status:"deleted"|"usage"|"not_found"|"silent";data?:string;outboxId?:string;instanceId?:string;}

// 레거시 master outer guard처럼 인자가 있는 후보만 허용합니다.
export function isPendantDeleteCommandCandidate(message:string|undefined):boolean{return message!==undefined&&/^\/펜던트삭제\s+.+$/.test(message);}
// 쉼표 형식의 대상 표시명과 가방 순번을 해석합니다.
export function parsePendantDeleteCommand(message:string):{targetName:string;index:bigint}|undefined{const match=/^\/펜던트삭제\s+(.+),\s*(\d+)$/.exec(message);if(match===null||match[1]!.trim()==="")return undefined;return{targetName:match[1]!.trim(),index:BigInt(match[2]!) };}
// 인자형 후보를 DB 대표 alias로 정규화합니다.
export function normalizePendantDeleteDispatchMessage(message:string):string{return isPendantDeleteCommandCandidate(message)?"/펜던트삭제":message;}
function eventKey(value:string):string{return value.length<=191?value:`sha256:${createHash("sha256").update(value).digest("hex")}`;}
function display(target:Target):string{const icon=target.entry.icon!==""&&target.entry.name.endsWith(target.entry.icon)?"":target.entry.icon;return`${target.entry.name}${icon}[${target.entry.grade}][⚒️${target.entry.durability}/${target.entry.maxDurability}](+${target.entry.upgrade})`;}
async function complete(transaction:DatabaseTransaction,input:{operationId:bigint;eventId:string;destinationId:string;operatorId:bigint;targetId:string|null;resultCode:string;data:string;result:PendantDeleteResult;summary:Record<string,unknown>}):Promise<PendantDeleteResult>{const outbox=await transaction.execute("INSERT INTO outbox_messages(operation_id,provider_code,destination_id,message_type,payload_json,status,available_at,created_at) VALUES (?,'iris',?,'text',?,'pending',UTC_TIMESTAMP(3),UTC_TIMESTAMP(3))",[input.operationId,input.destinationId,JSON.stringify({data:input.data})]);await transaction.execute("INSERT INTO command_executions(event_id,command_code,operation_id,execution_status,result_code,created_at,completed_at) VALUES (?,'PENDANT_DELETE',?,'completed',?,UTC_TIMESTAMP(3),UTC_TIMESTAMP(3))",[input.eventId,input.operationId,input.resultCode]);await transaction.execute("INSERT INTO command_audit(operation_id,actor_type,actor_id,target_type,target_id,action_code,result_code,reason,change_summary_json,created_at) VALUES (?,'admin_operator',?,'inventory_instance',?,'pet.pendant.delete',?,'Iris 총괄 운영자 /펜던트삭제',?,UTC_TIMESTAMP(3))",[input.operationId,input.operatorId,input.targetId,input.resultCode,JSON.stringify(input.summary)]);const result={...input.result,data:input.data,outboxId:outbox.insertId.toString()};await transaction.execute("UPDATE operations SET status='completed',result_json=?,completed_at=UTC_TIMESTAMP(3) WHERE id=?",[JSON.stringify(result),input.operationId]);return result;}

// 총괄 운영자가 대상 가방의 stable 펜던트를 원장 보존 상태로 제거합니다.
export class PendantDeleteService{constructor(private readonly database:DatabaseClient){} async handle(input:{eventId:string;externalUserId:string;destinationId:string;message:string}):Promise<PendantDeleteResult>{if(!isPendantDeleteCommandCandidate(input.message))return{status:"silent"};const operators=await this.database.query<Operator[]>(`SELECT operator.id operator_id FROM external_identities identity JOIN admin_operator_external_identities mapping ON mapping.external_identity_id=identity.id JOIN admin_operators operator ON operator.id=mapping.operator_id AND operator.status='active' JOIN admin_operator_roles operator_role ON operator_role.operator_id=operator.id JOIN admin_role_permissions permission ON permission.role_id=operator_role.role_id WHERE identity.provider_code='kakao' AND identity.external_user_id=? AND identity.status='linked' AND permission.permission_code='pet.pendant.delete' LIMIT 1`,[input.externalUserId]);const operator=operators[0];if(operator===undefined)return{status:"silent"};return this.database.withTransaction(async transaction=>{const key=eventKey(input.eventId);const prior=await transaction.query<Array<{result_json:string|PendantDeleteResult|null}>>("SELECT result_json FROM operations WHERE idempotency_scope='pendant.delete' AND idempotency_key=? FOR UPDATE",[key]);if(prior[0]?.result_json!=null)return typeof prior[0].result_json==="string"?JSON.parse(prior[0].result_json):prior[0].result_json;const operation=await transaction.execute("INSERT INTO operations(operation_key,idempotency_scope,idempotency_key,actor_type,actor_id,source_code,status,created_at) VALUES (?,'pendant.delete',?,'admin_operator',?,'iris','processing',UTC_TIMESTAMP(3))",[randomUUID(),key,operator.operator_id]);const command=parsePendantDeleteCommand(input.message);if(command===undefined)return complete(transaction,{operationId:operation.insertId,eventId:input.eventId,destinationId:input.destinationId,operatorId:operator.operator_id,targetId:null,resultCode:"usage",data:"사용법: /펜던트삭제 아이디, 펜던트가방번호",result:{status:"usage"},summary:{mutation:false}});const target=await this.resolveTarget(transaction,command.targetName,command.index);if(target===undefined)return complete(transaction,{operationId:operation.insertId,eventId:input.eventId,destinationId:input.destinationId,operatorId:operator.operator_id,targetId:null,resultCode:"not_found",data:"대상 펫 데이터가 없거나 해당 번호의 펜던트가 없습니다.",result:{status:"not_found"},summary:{targetName:command.targetName,sourceIndex:command.index.toString(),mutation:false}});const update=await transaction.execute("UPDATE inventory_instances SET status='consumed',version=version+1 WHERE id=? AND player_id=? AND status='owned' AND version=?",[target.row.instance_id,target.row.player_id,target.row.version]);if(update.affectedRows!==1n)throw new Error("PENDANT_DELETE_CONFLICT");await transaction.execute("INSERT INTO pendant_deletions(operation_id,player_id,inventory_instance_id,source_index,instance_version_before,instance_version_after,deleted_attributes_json) SELECT ?,player_id,id,?,?,?,attributes_json FROM inventory_instances WHERE id=?",[operation.insertId,command.index,target.row.version,target.row.version+1n,target.row.instance_id]);const data=`펜던트 삭제 완료\n대상: ${command.targetName}\n${display(target)}`;return complete(transaction,{operationId:operation.insertId,eventId:input.eventId,destinationId:input.destinationId,operatorId:operator.operator_id,targetId:target.row.instance_id.toString(),resultCode:"deleted",data,result:{status:"deleted",instanceId:target.row.instance_id.toString()},summary:{targetName:command.targetName,sourceIndex:command.index.toString(),instanceVersionAfter:(target.row.version+1n).toString()}});});}
 private async resolveTarget(transaction:DatabaseTransaction,targetName:string,index:bigint):Promise<Target|undefined>{const rows=await transaction.query<Row[]>(`SELECT instance.id instance_id,instance.player_id,instance.version,item.display_name item_name,JSON_UNQUOTE(JSON_EXTRACT(instance.attributes_json,'$.name')) name_value,JSON_UNQUOTE(JSON_EXTRACT(instance.attributes_json,'$.icon')) icon_value,JSON_UNQUOTE(JSON_EXTRACT(instance.attributes_json,'$.grade')) grade_value,JSON_UNQUOTE(JSON_EXTRACT(instance.attributes_json,'$.durability')) durability_value,JSON_UNQUOTE(JSON_EXTRACT(instance.attributes_json,'$.maxDurability')) max_durability_value,JSON_UNQUOTE(JSON_EXTRACT(instance.attributes_json,'$.upgrade')) upgrade_value FROM player_profiles profile JOIN players player ON player.id=profile.player_id AND player.status='active' JOIN player_pets pet ON pet.player_id=player.id JOIN inventory_instances instance ON instance.player_id=player.id AND instance.status='owned' JOIN item_definitions item ON item.id=instance.item_id AND item.active=TRUE WHERE profile.current_display_name=? AND COALESCE(JSON_UNQUOTE(JSON_EXTRACT(instance.attributes_json,'$.objectType')),JSON_UNQUOTE(JSON_EXTRACT(item.metadata_json,'$.objectType')))='pendant' FOR UPDATE`,[targetName]);const targets=rows.map(row=>({row,entry:{instanceId:row.instance_id.toString(),name:row.name_value??row.item_name,icon:row.icon_value??"",grade:row.grade_value??"",durability:BigInt(row.durability_value??"5"),maxDurability:BigInt(row.max_durability_value??"5"),upgrade:BigInt(row.upgrade_value??"0")}}));const selected=index>BigInt(targets.length)?undefined:sortPendantBagEntries(targets.map(v=>v.entry))[Number(index-1n)];return selected===undefined?undefined:targets.find(v=>v.entry.instanceId===selected.instanceId);}}
