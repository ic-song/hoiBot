import type { DatabaseClient } from "../database.js";
import { AdminStackGrantService,type AdminStackGrantCommand,type AdminStackGrantResult } from "./admin-stack-grant-service.js";
export type PetDungeonEntryGrantCommand=AdminStackGrantCommand;
export function isPetDungeonEntryGrantCommandCandidate(message:string|undefined):boolean{return message!==undefined&&/^\/던전\d*,.*$/.test(message);}
export function parsePetDungeonEntryGrantCommand(message:string):PetDungeonEntryGrantCommand{const match=/^\/던전(\d*)?,\s*(.+)$/.exec(message);if(match===null)return null;return{amount:BigInt(match[1]==null||match[1]===""?"1":match[1]),targetLegacyKey:match[2]!.trim()};}
// 공용 stack 지급 provider에 펫던전 입장권의 레거시 계약을 전달합니다.
export class PetDungeonEntryGrantService{constructor(private readonly database:DatabaseClient){}grant(input:{eventId:string;destinationId:string;operatorId:string;message:string}):Promise<AdminStackGrantResult>{return new AdminStackGrantService(this.database).grant({eventId:input.eventId,destinationId:input.destinationId,operatorId:input.operatorId,command:parsePetDungeonEntryGrantCommand(input.message)},{commandCode:"ADMIN_PET_DUNGEON_ENTRY_GRANT",itemCode:"ITEM-PET-DUNGEON-ENTRY-TICKET",itemName:"펫던전 입장권🌋",idempotencyScope:"admin.pet_dungeon_entry.grant",actionCode:"inventory.pet_dungeon_entry.grant",reasonCode:"ADMIN_PET_DUNGEON_ENTRY_GRANT",auditReason:"Iris 총괄 운영자 /던전",usageMessage:"올바른 형식으로 입력해 주세요. 예: /시련10, 유저아이디",invalidAmountMessage:"지급 개수는 1개 이상이어야 합니다.",noTargetMessage:"유저 아이디를 확인해 주세요.",formatGranted:(target,amount)=>`${target}님에게 시펫던전 입장권🌋 ${amount.toString()}개를 지급했습니다.`});}}
