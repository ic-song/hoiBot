import type {DatabaseClient} from "../database.js";import type {NormalizedIrisEvent} from "../integration/iris-normalizer.js";import {ApplicationError} from "../shared/application-error.js";import {isDiamondPassCommandCandidate} from "./diamond-pass-command.js";import {DiamondPassService} from "./diamond-pass-service.js";
// Iris 다이아패스 명령을 stable registry service에 연결합니다.
export class DiamondPassIrisHandler{public constructor(private readonly database:DatabaseClient){}public async execute(event:NormalizedIrisEvent):Promise<{message:string;room:string;replayed:boolean;outboxId:string}>{if(!isDiamondPassCommandCandidate(event.message))throw new ApplicationError("DIAMOND_PASS_COMMAND_INVALID","다이아패스 명령 형식을 확인해 주세요.",422);if(!event.userId||!event.channelId)throw new ApplicationError("DIAMOND_PASS_IDENTITY_REQUIRED","사용자 식별 정보를 확인할 수 없습니다.",422);const r=await new DiamondPassService(this.database).execute({eventId:event.eventId,externalUserId:event.userId,destinationId:event.channelId,message:event.message!});return{message:r.data,room:event.channelId,replayed:r.replayed,outboxId:r.outboxId};}}
