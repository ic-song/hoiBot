# 패키지 canonical GAP 교정 계약

- 동결 원본 판정 `467/46`과 PACKAGE gap `44`는 변경하지 않습니다.
- exact `LEGACY_JS | member.bag | target display` 근거가 있는 STACK occurrence `10`건만 versioned sidecar overlay로 연결합니다.
- 관리 웹은 frozen status와 effective overlay status를 분리해 표시합니다.
- effective 결과는 resolved `477`, residual STACK `36`, residual PACKAGE `44`, conflict `0`입니다.
- 연결된 4개 definition의 `active=false`는 별도 availability로 표시하며 지급 가능으로 추론하지 않습니다.
- 표시명만으로 identity를 병합하거나 신규 identity를 만들지 않습니다.
- Gate 8, 운영 DB, `feature/prod`, legacy 파일은 변경하지 않습니다.
