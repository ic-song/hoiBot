# WBS734 canonical furniture/home validation

- Migration `445_object_furniture_home_canonical_model.sql` is additive and runs after `444_canonical_item_inventory.sql`; legacy `furniture_definitions`, `owned_furniture`, and `furniture_inventory_instances` are not changed.
- Migration `450_object_furniture_market_active_listing.sql` runs after 445 and replaces the historical `(owned_furniture_id)` unique key with MariaDB 11.4 STORED generated `active_owned_furniture_id`; cancelled/sold rows remain immutable history while the generated-key UNIQUE permits at most one `active` row per owned furniture.
- New canonical definition rows keep exact UTF-8 display names and common `purchase_price`, `base_charm`, and `charm_per_enhancement` values only once.
- Each owned copy has its own `owned_furniture_id`; `enhancement_level` and `ownership_status` are the only business state stored on the instance.
- Home reads join the current definition and calculate `base_charm + enhancement_level * charm_per_enhancement`; no `final_charm` or snapshot column exists in migration 445. `ownership_status` is the lifecycle authority (`bag`, `placed`, `listed`, `sold`, `removed`); a placement is the `placed`-state detail and must exist only while that status is `placed`. Market rows are similarly related to `listed`; transition services write state, relation, history, and replay in one transaction.
- `object_furniture_operation_replays` protects grant replay through `(player_id, idempotency_scope, idempotency_key)` in the same transaction as instance creation.
- Local MariaDB execution is intentionally not attempted: no development `.env` / database target is provisioned in this isolated worktree. Contract, typecheck, build, and static SQL checks are required before an isolated MariaDB probe.
- This branch does not contain `444_canonical_item_inventory.sql`; `canonical_players` is therefore declared only as an integration dependency and migration 445 must not run independently. Foreman will validate it after merging the 444 provider commit `06c97afb`. Legacy command consumer conversion, RAW→Domain Import, production-parity comparison, and Shadow execution are not completed in this slice. They are owned by the consumer/import/validation waves; this evidence must not be read as their completion.
- Fresh MariaDB 11.4 migration/reapply/rollback validation is intentionally not claimed here; it remains central integration work.
