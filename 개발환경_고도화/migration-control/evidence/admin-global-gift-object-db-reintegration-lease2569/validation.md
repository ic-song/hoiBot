# WBS752 validation

- `npm run typecheck`: PASS.
- focused: `admin-global-gift-service.test.ts` + `object-data-model-contract.test.ts`, 27/27 PASS.
- isolated MariaDB fresh apply: migration 001~479 중 repository의 467개 migration 적용, 479 포함 PASS.
- isolated MariaDB scenario: 실제 FK/no-logical-id introspection, 권한·11채널 fail-close, concurrency/replay, restart+SHADOW, inbound channel payload conflict, outbox payload/status drift, recipient 동일-count player 치환/수량 pair 변조 및 stack owner/operation item 관계 drift, ambiguous commit 단일 fresh transaction snapshot 전체 evidence reconcile, active member mapping 누락, overflow rollback 9/9 PASS.
- ambiguous ACK snapshot: recovery phase의 pool autocommit query/execute를 test double에서 즉시 거부하고 `withTransaction` 1회와 그 transaction의 전체 SELECT trace를 확인했다. 첫 operation SELECT 직후 별도 connection에서 recipient 수량 drift를 commit한 상태에서도 MariaDB transaction snapshot이 이전의 일관된 operation/recipient/channel/outbox를 검산하고 혼합 snapshot을 만들지 않음을 확인한 뒤 fixture를 원복했다.
- rollback: durable receipt 존재 시 guard PASS; source binding drift fail-close PASS; 동일 예약 ID(`j7uyw6vc`/`phk8c656`) 선재 exact binding의 INSERT_USER/item_id를 forward/rollback 전후 불변으로 확인 PASS; fresh seed는 provenance marker=true일 때만 rollback 삭제 PASS. provenance=true이면 rollback 시작 전에 `INFORMATION_SCHEMA.KEY_COLUMN_USAGE`로 `canonical_item_definitions(item_id)`를 참조하는 실제 FK 전부와 추가 definition import를 검산하며, 무관 `canonical_owned_item_instances` FK fixture가 있을 때 table 5개·item·instance 모두 불변(DML/DDL 0)으로 fail-close함을 확인했다. 479 reapply PASS; migration runner restart checksum/re-entry 467 PASS.
- provenance PK `n89yb2ye`는 승인된 `@paralleldrive/cuid2` length=8 generator로 오프라인 생성한 opaque CUID8이며 의미 기반 singleton ID를 사용하지 않는다.
- object manifest: migration 30개, 표준 table 104개 계약 PASS. 신규 5개 table은 descriptive CUID8 PK와 audit4 KST CHAR(19)를 사용하며, 도메인 관계가 있는 4개 table의 exact CUID/operation-key FK 7개를 INFORMATION_SCHEMA에서 확인했다. provenance table은 migration이 생성한 catalog seed 여부만 기록하므로 외부 도메인 FK 없이 opaque CUID8 PK와 boolean marker만 가진다.
- item identity: 런타임은 `canonical_item_definition_imports`의 exact source tuple로 `item_id`를 해석하고 이후 비교·FK·ownership은 `item_id`만 사용한다. `ITEM-FREE-HOI-SUPPORT-02`는 migration의 legacy 경계 증거일 뿐 runtime source에는 존재하지 않음을 focused test로 고정했다.
- save 의미: active-member snapshot, 대상별 +1, typed receipt, 11개 ordered outbox, command terminal이 RFA02/RFA03 root transaction 하나에서 commit된다. active legacy 회원 중 canonical mapping이 하나라도 없으면 mutation 전에 전체 fail-close한다.
- recipient 무결성: `recipient_sequence/player_id/item_id/owned_item_stack_id/quantity_before/quantity_after` 정렬 projection의 SHA-256을 operation `recipient_fingerprint`와 terminal result 양쪽에 bind한다. 정상 replay와 ambiguous-commit 재조회 모두 전체 행을 재계산하고 `recipient.player_id = stack.player_id`, `recipient.item_id = stack.item_id = operation.item_id = terminal.result.itemId`를 함께 검산한다. count가 같아도 단일행 치환·수량 변조·stack owner 치환·operation item 치환을 거부한다.
- outbox 무결성: FK처럼 보이는 논리 `outbox_message_id`는 두지 않는다. canonical operation의 `operation_key`가 applied `operations(operation_key)` UNIQUE를 동일명·동일형 실제 FK로 참조한다. replay/RFA02 ambiguous-commit 재조회는 이 operation 아래 outbox 11개를 생성 순서로 channel snapshot의 sequence/destination/payload hash와 대조하고 delivery status 허용 집합 및 terminal receipt까지 검산한다. INFORMATION_SCHEMA에서 해당 FK와 신규 schema FK 총 7개를 확인했다.
- external dependency: validator에는 applied migration 002의 `operations(id PK, operation_key UNIQUE)` 실제 최소 shape만 pin했고 신규 권위나 공용 DB/runtime 동작은 추가하지 않았다. shape drift 계약 테스트 PASS.
- legacy `main.js`, `Info.js`, `data/`는 수정하지 않았다.
- 이전 source 독립 리뷰 승인 범위: source `0708f69f` 구현과 `edae6a6b` evidence, 이어진 `7539d6ee` provenance/recipient 보완까지 P0=0/P1=0/P2=0. integration 후속 리뷰에서 ambiguous ACK 다중 autocommit read와 provenance 외부 FK preflight 누락 P1을 발견했다. source `7539d6ee` 위 현재 후속 diff는 두 finding을 보완했으며 최신 독립 읽기전용 재리뷰에서 P0=0/P1=0/P2=0 승인을 받았다.

검증 이력 주의: 개발 중 실수로 full test 명령을 한 번 시작했으나 즉시 중단했으며 의존성 미연결 출력만 발생했다. 해당 실행은 T3 또는 성공 증거로 주장하지 않는다. 이후 허용된 focused test만 별도로 실행해 위 결과를 얻었다.
