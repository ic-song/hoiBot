# Wave5 scenarios

- One consumer × five required scenarios = five new receipts; prior `40` retained, combined `45`.
- `READ_POSITIVE`, `EXACT_OUTPUT`, `SOURCE_DOMAIN_DML_ZERO`: exact SQL/`player01` query value/result, computed `finalCharm=140`, DML0, empty lock order, `READ_ONLY`.
- `NEGATIVE_GUARD`: invalid `bad` player ID returns `CANONICAL_FURNITURE_ID_INVALID` with query count `0` and `GUARD_REJECTED`.
- `RESTART_CONSISTENCY`: two fresh child processes and distinct module executions produce identical results.
- Drift defenses cover locator, input/query/mock/result player relation, full SQL, mock DB row and calculated result.
