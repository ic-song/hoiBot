# hoiBot 고도화 확정 결정

최종 갱신일: 2026-08-07

이 문서는 hoiBot 고도화에서 사용자가 명시적으로 확정한 결정의 단일 기준이다.
`MEMORY.md` 또는 `references/` 문서와 내용이 충돌하면 이 문서를 우선한다.

## 관리 규칙

1. 사용자가 명시적으로 확정한 내용만 등록한다.
2. 추천, 추측, 검토안, 미검증 내용은 등록하지 않는다.
3. 결정을 변경할 때 기존 기록을 삭제하거나 조용히 덮어쓰지 않는다.
4. 기존 결정은 `SUPERSEDED`로 바꾸고 대체 결정 ID와 변경일을 기록한다.
5. 현재 적용할 결정은 `ACTIVE` 상태만 사용한다.

## 결정 기록

`ACTIVE`만 현재 적용한다. `SUPERSEDED`는 변경 이력을 보존하기 위한 기록이다.

### DEC-001: PC redroid 실행환경

- 결정일: 2026-08-03
- 상태: `ACTIVE`
- 결정: hoiBot 고도화 실행환경은 PC의 redroid 구성만 사용한다.
- 구성: `Windows -> Hyper-V -> Ubuntu/Linux VM -> Docker -> redroid -> KakaoTalk + Iris`
- 근거: 사용자가 PC에서 redroid를 사용한다고 확인하고 해당 구성을 고도화 방향으로 확정했다.
- 적용 범위: 실행환경 설계, 설치, 테스트, 운영 전환 계획

### DEC-002: 기준 영상

- 결정일: 2026-08-03
- 상태: `ACTIVE`
- 결정: YouTube `Iris를 이용한 봇 만들기`를 PC/redroid 환경 구성의 기준 영상으로 사용한다.
- 링크: https://www.youtube.com/watch?v=H43VTOsKDXY
- 적용 범위: Hyper-V, Linux, Docker, redroid, Iris 설치 흐름

### DEC-003: hoiBot Server 구현 스택

- 결정일: 2026-08-03
- 상태: `ACTIVE`
- 결정: hoiBot Server는 `TypeScript + Node.js 24 LTS + Fastify 5`로 구현한다.
- 근거: 기존 JavaScript 게임 로직과 `개발환경_고도화/runtime/`의 TypeScript/Fastify 서버 기반을 점진적으로 재사용한다.
- 적용 범위: HTTP/WebSocket 서버, Iris adapter, 명령 처리, 서버 측 데이터 접근 계층

### DEC-004: 고도화 문서 관리 범위

- 결정일: 2026-08-03
- 상태: `ACTIVE`
- 결정: 고도화 관련 메모리와 문서는 `개발환경_고도화/` 내부에서만 관리한다.
- 결정: 기술/API 및 조사성 정보는 `references/` 아래의 영문 Markdown으로 관리한다.
- 적용 범위: 세션 메모리, 결정 기록, 기술 참고자료, 검증 결과

### DEC-005: 고도화 전용 브랜치

- 결정일: 2026-08-03
- 상태: `ACTIVE`
- 결정: 고도화 관련 문서, 서버, redroid/Iris 연동 작업은 `feature/modernization` 브랜치에서만 진행한다.
- 기준: 새 작업을 시작하기 전에 최신 `feature/prod`를 확인하고 `feature/modernization`에 반영한다.
- 운영 반영: 고도화 변경은 사용자가 명시적으로 요청한 경우에만 검증된 커밋을 `feature/prod`에 반영한다.
- 적용 범위: `개발환경_고도화/` 내부의 문서, `runtime/`, 향후 hoiBot Server 및 adapter 구현

### DEC-006: Iris 기반 고도화

- 결정일: 2026-08-03
- 상태: `SUPERSEDED`
- 대체 결정: `DEC-021` (2026-08-04)
- 결정: hoiBot은 Iris를 사용해 고도화한다.
- 역할: Iris는 redroid 안에서 KakaoTalk 메시지 감지와 답장 전송을 담당한다.
- 역할: hoiBot Server는 Iris의 HTTP/WebSocket 이벤트를 받아 명령과 게임 로직을 처리하고 Iris `/reply`로 응답한다.
- 전환 시점: `ASAP`. 별도 일정까지 기다리지 않고 구현과 검증이 완료된 기능부터 가능한 한 빠르게 순차 전환한다.
- 적용 범위: KakaoTalk 입출력 adapter, 이벤트 정규화, 응답 전송, 기존 MessengerBot 의존 로직의 점진적 이전

### DEC-007: 고도화 검증 및 구현 순서

- 결정일: 2026-08-03
- 상태: `ACTIVE`
- 결정: 서버 본구현보다 redroid/Iris 클라이언트의 실제 연동 검증을 먼저 수행한다.
- 1단계: redroid 안의 KakaoTalk + Iris에서 테스트 명령을 보내 hoiBot Server와 연결 및 기본 왕복을 확인한다.
- 2단계: 일반 메시지뿐 아니라 Iris가 제공하는 각종 이벤트와 부가 데이터가 서버로 전송되는지 확인하고 실제 수신 범위를 기록한다.
- 3단계: 1~2단계에서 확인한 실제 payload와 제약을 기준으로 hoiBot Server를 구현한다.
- 적용 범위: redroid/Iris 연동 검증, 이벤트·payload 조사, hoiBot Server 구현 착수 기준

### DEC-008: `/ping` 감지 검증 상한

- 결정일: 2026-08-03
- 상태: `ACTIVE`
- 결정: 앞으로 `/ping` 반복 감지 검증은 서로 다른 이벤트 10건까지만 집계하고 10건 확인 즉시 종료한다.
- 적용 범위: 연결 안정성 확인, 반복 감지 시험, 검증 결과 보고

### DEC-009: `/ping` 서버 연결 확인 응답

- 결정일: 2026-08-03
- 상태: `ACTIVE`
- 결정: KakaoTalk에서 정확히 `/ping`을 입력하면 Iris가 hoiBot Server로 이벤트를 전달하고 서버는 같은 방에 `발신자이름 pong`으로 답한다.
- 실행 조건: 메시지 전체가 `/ping`과 정확히 일치해야 하며 추가 문자가 붙은 입력은 실행하지 않는다.
- 적용 범위: redroid/Iris/hoiBot Server 연결 확인 명령

### DEC-010: hoiBot Server 프로젝트 위치

- 결정일: 2026-08-03
- 상태: `ACTIVE`
- 결정: 고도화용 Node.js hoiBot Server 프로젝트는 `개발환경_고도화/runtime/` 내부에서만 관리한다.
- 근거: 고도화 관련 파일을 `개발환경_고도화/` 내부에 격리한다.
- 적용 범위: 서버 소스, 패키지 설정, 테스트, 실행 문서와 로컬 런타임 설정

### DEC-011: Iris 이벤트 정보와 서버 매핑 분리

- 결정일: 2026-08-03
- 상태: `ACTIVE`
- 결정: Iris 이벤트의 실제 검증 근거와 hoiBot Server 구현용 매핑 정보를 별도 파일로 관리한다.
- 검증 기준: `references/IRIS_EVENT_CAPABILITY_MATRIX.md`
- 서버 구현 참고: `references/IRIS_SERVER_EVENT_MAPPING.json`
- 관리 원칙: 실관측·원본 코드·DB 근거가 없는 항목은 `unverified` 또는 `not_direct` 상태로 유지한다.
- 적용 범위: Iris adapter, 이벤트 정규화, 명령 처리 전처리와 향후 테스트 설계

### DEC-012: 수신 이미지 테스트방 전달

- 결정일: 2026-08-03
- 상태: `SUPERSEDED`
- 대체 결정: `DEC-013` (2026-08-03)
- 결정: Iris에서 단일 이미지가 수신되면 `/ping` 연결을 검증한 테스트방으로 전달한다.
- 실행 조건: `type=2`, `isMine=false`인 수신 이벤트만 처리하며 대상 방 ID는 코드나 문서가 아닌 실행 환경에서 관리한다.
- 안전 범위: Kakao CDN HTTPS 이미지, 설정된 용량·시간 제한을 통과한 경우만 전달한다. 다중 이미지는 별도 실검증 전까지 제외한다.
- 적용 범위: hoiBot Lite Server의 Iris 이미지 수신·전달 검증

### DEC-013: 지정 계정 이미지 테스트방 전달

- 결정일: 2026-08-03
- 상태: `SUPERSEDED`
- 대체 결정: `DEC-014` (2026-08-03)
- 결정: 카카오톡 계정이 하나인 현재 테스트 환경에서는 사용자가 직접 보낸 단일 이미지만 `/ping` 테스트방으로 전달한다.
- 식별 조건: `/ping`을 보낸 계정의 `user_id`가 일치하는 이미지만 처리하며 ID는 코드나 문서가 아닌 실행 환경에서 관리한다.
- 반복 방지: 테스트방에서 감지된 이미지는 다시 전달하지 않는다. 따라서 같은 계정의 전송 이벤트가 `isMine=true`여도 다른 방에서 발생한 경우 처리할 수 있다.
- 안전 범위: Kakao CDN HTTPS 이미지, 설정된 용량·시간 제한을 통과한 경우만 전달한다. 다중 이미지는 별도 실검증 전까지 제외한다.
- 적용 범위: hoiBot Lite Server의 단일 계정 이미지 전달 검증

### DEC-014: 모든 단일 이미지 테스트방 전달

- 결정일: 2026-08-03
- 상태: `SUPERSEDED`
- 대체 결정: `DEC-015` (2026-08-03)
- 결정: Iris가 감지한 모든 방의 단일 이미지를 `/ping` 테스트방으로 전달한다.
- 반복 방지: 테스트방에서 감지된 이미지는 다시 전달하지 않는다.
- 안전 범위: Kakao CDN HTTPS 이미지, 설정된 용량·시간 제한을 통과한 경우만 전달한다. 다중 이미지는 별도 실검증 전까지 제외한다.
- 적용 범위: hoiBot Lite Server의 단일 이미지 전달 검증

### DEC-015: 이미지 자동 전송 OFF

- 결정일: 2026-08-03
- 상태: `ACTIVE`
- 결정: Iris 이미지 감지는 유지하되 테스트방 자동 전송은 끈다.
- 실행 상태: `IRIS_IMAGE_FORWARD_ROOM_ID`를 설정하지 않은 상태로 서버를 실행한다.
- 재활성화: 사용자가 이미지 전송 ON을 명시적으로 요청할 때만 대상 테스트방을 다시 설정한다.
- 적용 범위: hoiBot Lite Server 이미지 자동 전달 기능

### DEC-016: hoiBot Server 전용 DB

- 결정일: 2026-08-03
- 상태: `ACTIVE`
- 결정: redroid KakaoTalk DB와 별개인 hoiBot Server 전용 데이터베이스로 MariaDB를 사용한다.
- 역할: 기존 `/sdcard/호이랜드/` JSON에 저장된 게임·운영 데이터를 서버 측 MariaDB로 점진적으로 이전한다.
- 분리 원칙: redroid KakaoTalk DB는 Iris 이벤트·조회 원본이며 hoiBot 게임 데이터 저장소로 사용하지 않는다.
- 후속 결정: DB 접근 구조는 `DEC-022`, 최종 배치는 `DEC-024`, 상세 스키마·마이그레이션·백업 방식은 해당 기준 문서에서 관리한다.
- 적용 범위: hoiBot Server 영속성, 데이터 이전, 트랜잭션과 백업 설계

### DEC-017: PC 간 동일 환경 재현 목표

- 결정일: 2026-08-03
- 상태: `ACTIVE`
- 결정: 집 PC에서 먼저 구축·검증한 고도화 환경을 이후 운영 PC에도 같은 구조로 쉽게 구성할 수 있어야 한다.
- 관리 원칙: 공통 구성·스크립트·마이그레이션은 저장소에서 관리하고, PC별 주소·토큰·비밀번호·식별자와 운영 데이터는 Git 밖에서 관리한다.
- 적용 범위: Windows/Hyper-V/Linux/Docker/redroid/Iris/hoiBot Server/MariaDB 설치, 검증, 백업·복구 절차

### DEC-018: hoiBot Server 상시 실행

- 결정일: 2026-08-04
- 상태: `ACTIVE`
- 결정: hoiBot Server는 수동 필요 시 실행이 아니라 상시 실행되도록 구성한다.
- 관리 원칙: 현재 PC에서는 자동 시작과 장애 재시작을 적용하고, 최종 운영 배치가 확정되면 동일 요구사항을 Windows 작업 스케줄러 또는 Linux/Docker 서비스 관리 방식으로 재현한다.
- 적용 범위: Node.js 서버 시작, 장애 복구, PC 재부팅 후 자동 실행

### DEC-019: 중앙 서버와 외부 관리·연동 확장

- 결정일: 2026-08-04
- 상태: `ACTIVE`
- 결정: 기존 JavaScript + JSON 기반 hoiBot 기능과 데이터를 hoiBot Server로 점진적으로 이전한다.
- 관리 기능: 별도 관리 홈페이지에서 hoiBot Server API를 통해 게임·운영 데이터를 조회하고 변경할 수 있어야 한다.
- 외부 연동: Discord 및 기타 외부 API를 동일한 hoiBot Server의 연동 계층을 통해 연결할 수 있어야 한다.
- 경계 원칙: 관리 홈페이지와 외부 연동은 MariaDB에 직접 접속하지 않고, 인증·권한·검증·감사 기록을 적용한 서버 API를 사용한다.
- 적용 범위: 서버 모듈 경계, MariaDB 스키마, 관리자 API, 관리 홈페이지, Discord 및 외부 API adapter, 기존 JSON 데이터 이전

### DEC-020: 기존 JSON 기반 확장형 RDB 설계

- 결정일: 2026-08-04
- 상태: `ACTIVE`
- 결정: MariaDB 스키마는 현재 hoiBot JSON 데이터와 실제 저장 흐름을 기준으로 설계하되, JSON 파일별 복제 테이블이 아니라 확장 가능한 도메인별 관계형 모델로 구성한다.
- 식별 원칙: 닉네임 대신 내부 식별자를 기준으로 하고 KakaoTalk, Discord, 관리자 계정 및 기타 외부 식별자는 별도 매핑한다.
- 데이터 원칙: 재화·아이템·소유권 변경은 트랜잭션과 원장·감사 기록을 적용하며, JSON 컬럼은 과도기 데이터 또는 구조가 실제로 가변적인 메타데이터에만 제한한다.
- 공통화 원칙: 코드에 하드코딩된 공유 상태·유형·사유 코드는 공통코드로, 속성과 관계가 있는 아이템·스킬·재화 등은 전용 도메인 카탈로그로, 운영 중 조정할 수 있는 제한·확률·보상은 검증·버전·적용시점을 갖는 설정으로 분리한다.
- 적용 범위: 기존 JSON 분석, MariaDB 논리·물리 설계, 데이터 이전, 관리 홈페이지와 외부 연동 확장

### DEC-021: 별도 환경 완성 후 Iris 일괄 운영 전환

- 결정일: 2026-08-04
- 상태: `ACTIVE`
- 결정: 기능 구현과 검증은 `feature/modernization`의 별도 테스트 환경에서 수직 기능 단위로 진행하되, 운영 전환은 전체 기능 검증 후 기존 Rhino를 중단하고 Iris 서버로 일괄 수행한다.
- 전환 절차: 최종 JSON 스냅샷과 checksum 생성, Rhino 변경 동결, MariaDB import/reconciliation, Rhino 중단, Iris 연결, 읽기 전용 smoke, mutation 일괄 활성화 순서로 진행한다.
- 롤백: mutation 활성화 전에는 JSON/Rhino로 복귀할 수 있다. mutation 활성화 후에는 이전 서버 이미지 또는 MariaDB backup/point-in-time 복구만 사용하며 오래된 JSON으로 역전환하지 않는다.
- 적용 범위: 구현 순서, 테스트 환경, 운영 전환, 롤백

### DEC-022: 서버 애플리케이션·DB 접근 구조

- 결정일: 2026-08-04
- 상태: `ACTIVE`
- 결정: 서버 구조는 `Controller/Adapter -> Application Service -> Domain Policy -> Repository -> MariaDB`로 구성한다.
- DB 접근: MariaDB Connector와 파라미터 SQL을 사용하고 ORM은 도입하지 않는다.
- 경계 원칙: Iris, 관리자 홈페이지, Discord와 외부 API는 동일한 Application Service를 사용한다.
- 적용 범위: hoiBot Server 모듈, 트랜잭션, API와 외부 adapter

### DEC-023: 첫 수직 기능과 관리자 기반

- 결정일: 2026-08-04
- 상태: `ACTIVE`
- 결정: 첫 읽기 수직 기능은 기존 출력 전체를 재현하는 KakaoTalk `/내정보`이며, 첫 변경 기능은 관리자 `/서버이동`이다.
- 관리 순서: 관리자 API를 먼저 구현하고 관리 화면은 React + Vite SPA로 후속 구축한다.
- 인증: 첫 관리 기능은 로컬 운영자 계정, Argon2id 비밀번호, DB 세션과 RBAC를 사용한다. 일반 사용자 HTTP 로그인은 이번 범위에서 제외한다.
- 적용 범위: profile read model, 관리자 인증·조회·서버 변경 API, 관리 홈페이지

### DEC-024: 재현 가능한 최종 배치

- 결정일: 2026-08-04
- 상태: `ACTIVE`
- 결정: 기본 운영 배치는 Hyper-V Ubuntu의 Docker Compose에 hoiBot Server와 MariaDB를 함께 배치하고 redroid는 별도 service boundary로 유지한다.
- 재현 기준: 집 PC와 운영 PC는 동일 image digest, migration version과 `.env` key contract를 사용한다.
- 적용 범위: Docker image, Compose, 백업·복구와 PC 간 재구축

### DEC-025: 시스템 계정·캐릭터·외부 계정 식별 규칙

- 결정일: 2026-08-05
- 상태: `ACTIVE`
- 결정: 시스템 계정과 캐릭터는 1:1이며 시스템 계정 하나에 KakaoTalk 계정 하나와 Discord 계정 하나만 연결한다. 각 외부 계정 key도 하나의 시스템 계정에만 연결할 수 있다.
- 이름 규칙: 시스템 계정 이름은 전역에서 고유하고 `한글 2글자 + 공백 1칸 + 남 또는 여` 형식만 허용한다. 시스템 계정 이름과 캐릭터 이름은 동일하다.
- 명령 조건: 등록된 provider 계정 key와 현재 provider 닉네임이 모두 일치해야 명령을 실행한다. 닉네임만으로 계정을 찾거나 승인하지 않는다.
- 동시성: KakaoTalk과 Discord는 같은 캐릭터 데이터를 사용하며 동일 시스템 계정의 명령은 provider와 관계없이 순차 실행한다.
- 상세 규정: `references/SYSTEM_POLICIES.md`
- 적용 범위: 회원, 캐릭터, external identity, KakaoTalk·Discord adapter, 명령 인증과 동시성

### DEC-026: 사이트 가입과 provider 코드 연동

- 결정일: 2026-08-05
- 상태: `ACTIVE`
- 결정: 사용자가 사이트에서 먼저 회원가입하고 발급받은 코드를 KakaoTalk 봇에 입력해 계정을 활성화하고 KakaoTalk key를 연결한다.
- 코드 정책: 30분간 유효한 1회용 코드이며 5회 실패 또는 성공 시 즉시 폐기한다. 원문은 저장하지 않고 hash만 저장한다.
- 재연동: 가입 후 `내정보 > 계정`에서 같은 코드 방식으로 KakaoTalk 재연동과 Discord 연동·재연동을 제공한다. 새 계정 인증 성공 시점에만 기존 연결을 교체한다.
- 적용 범위: 사이트 회원가입, 사용자 인증, provider 연결·재연결, KakaoTalk·Discord bot

### DEC-027: 계정 이름 변경·탈퇴·제재 정책

- 결정일: 2026-08-05
- 상태: `ACTIVE`
- 이름 변경: 전용 변경권을 사용해 앞의 한글 두 글자만 변경할 수 있고 성별은 변경할 수 없다. provider key 연결은 유지하며 닉네임이 새 시스템 이름과 일치할 때까지 명령을 차단한다.
- 탈퇴: 탈퇴 신청 후 30일간 사용을 차단하고 사이트에서 복구할 수 있다. 유예 중 이름과 provider key는 예약하며 회원가입 시 이미 가입한 사용자로 처리한다.
- 영구 삭제: 30일 후 개인정보·게임 데이터·identity를 삭제하고 이름과 일반 provider key를 재사용 가능하게 한다. 원장·감사는 익명화해 보존한다.
- 제재: 기간 정지는 종료 시 자동 해제하며 영구 정지는 운영자만 해제한다. 영구 정지 계정은 탈퇴할 수 있지만 provider key hash를 차단 목록에 보존해 재가입을 막는다.
- 적용 범위: 계정 상태, 로그인, 이름 변경, 탈퇴·복구, 삭제·익명화, 운영 제재

### DEC-028: 정수 재화와 운영자 정정 규칙

- 결정일: 2026-08-05
- 상태: `ACTIVE`
- 결정: 포인트·다이아·아이템 수량 등 재화는 정수만 사용하고 음수 잔액을 허용하지 않는다. 레거시 소수 재화와 비율 계산 결과는 일반 반올림한다.
- 정정: 기존 원장을 수정·삭제하지 않고 원본 작업과 연결된 반대 원장을 추가한다. 권한 있는 운영자 한 명이 필수 사유를 입력해 처리할 수 있다.
- 미회수: 즉시 회수하지 못한 재화·아이템은 별도 사건으로 기록하며 자동 상계하지 않는다. 사용자는 정상 이용하고 운영자가 전용 관리 화면에서 수동 회수·일부 회수·포기한다.
- 적용 범위: currency, inventory ledger, 운영자 지급·차감, 감사, 미회수 재화 관리

### DEC-029: 인벤토리·제한 가방·우편 정책

- 결정일: 2026-08-05
- 상태: `ACTIVE`
- 저장 방식: 재료·소모품은 수량형 stack으로, 장비·고유 자산은 개별 instance로 관리한다.
- 일반 가방: 전체 칸 제한은 없고 필요한 아이템만 개별 최대 보유량을 둔다. 최대치 초과분은 우편으로 보낸다.
- 제한 가방: 미니펫과 가구는 같은 종류라도 개체 하나당 한 칸을 사용한다. 종류·조건별 용량 규칙과 중복 적용 방식을 별도로 관리하고, 가득 찬 경우 신규 개체를 우편으로 보낸다.
- 용량 감소: 기간제 혜택 만료로 초과 보유가 발생해도 기존 개체는 유지하고 신규 추가와 우편 수령만 제한한다.
- 우편: 수령 기한이 없고 가능한 수량만 부분 수령할 수 있다. 수령 완료 우편은 사용자 화면에서 제거하되 원장·감사 이력은 보존한다.
- 적용 범위: item catalog, inventory, mini-pet, furniture, capacity policy, mail

### DEC-030: 거래소 자산 보관과 판매 대금 우편 지급

- 결정일: 2026-08-05
- 상태: `ACTIVE`
- 보관: 판매 등록 자산은 판매자 가방에서 거래소 보관 상태로 이동하며 판매·취소 전까지 사용·강화·이중 등록할 수 없다.
- 구매: 구매자 재화 차감, 소유권 이전, 수수료 원장, 거래 완료, 판매자 정산 우편 생성을 하나의 트랜잭션으로 처리한다.
- 정산: 판매 대금은 판매자 잔액에 즉시 지급하거나 별도 정산함에 두지 않고, 수수료를 제한 금액을 수령 기한 없는 우편으로 지급한다.
- 반환: 취소·만료 자산은 가방으로 반환하고 제한 가방이 가득 차면 우편으로 반환한다.
- 미확정: 신규 시스템의 등록 만료 기간과 레거시 등록·취소 수수료 유지 여부는 `references/SYSTEM_POLICIES.md`의 열린 항목으로 둔다.
- 적용 범위: market listing, custody, purchase, fee ledger, settlement mail, cancel/expiry

### DEC-031: 사용자 사이트 로그인·동의·세션 정책

- 결정일: 2026-08-05
- 상태: `ACTIVE`
- 로그인 정보: 변경 불가능한 별도 로그인 ID와 비밀번호를 사용한다. 로그인 ID는 영문 소문자·숫자 6~20자이며 비밀번호는 8~64자, 영문과 숫자를 각각 하나 이상 포함한다.
- 미인증 계정: 사이트에서 시스템 이름을 검사한 후 `PENDING_KAKAO_LINK` 계정과 30분 인증 코드를 생성한다. 24시간 동안 사이트 로그인으로 코드를 재발급할 수 있고 미인증 상태가 24시간을 넘으면 계정·비밀번호 hash·이름 예약을 삭제한다.
- 동의: `SUPERSEDED by DEC-034` — 이용약관과 개인정보 처리 안내에 각각 필수 동의를 받기로 했던 결정 중 개인정보 처리방침 동의 부분은 제거한다.
- CAPTCHA: `SUPERSEDED by DEC-032` — 모든 사이트 로그인에 외부 CAPTCHA를 적용하기로 했던 결정은 1차 구현 범위에서 제외한다.
- 비밀번호 복구: 이메일·전화번호 없이 이미 연결된 KakaoTalk 또는 Discord의 1회용 코드 인증으로 처리하고 성공 시 기존 세션을 모두 폐기한다.
- 세션: 비활성 24시간, 절대 7일 만료를 적용하고 동시 로그인 수는 제한하지 않는다. 일반 로그아웃은 현재 세션만 폐기하며 개별 세션 관리 화면은 제공하지 않는다.
- 적용 범위: 사용자 회원가입·로그인, 약관 동의, 비밀번호 재설정, 사용자 세션

### DEC-032: 1차 사용자 인증에서 CAPTCHA 제외

- 결정일: 2026-08-05
- 상태: `ACTIVE`
- 결정: 초기 회원가입·로그인 구현에는 CAPTCHA 검증, provider adapter와 관련 환경설정을 포함하지 않는다.
- 후속 원칙: abuse 방어가 실제로 필요해지면 별도 사용자 결정과 위협 검토 후 새 결정으로 추가한다.
- 적용 범위: 사용자 회원가입·로그인 API와 홈페이지

### DEC-033: 홈페이지 계정 진입 메뉴

- 결정일: 2026-08-05
- 상태: `ACTIVE`
- 결정: 홈페이지 우측 상단은 비로그인 상태에서 `로그인`, 로그인 상태에서 `내정보`를 표시한다. `회원가입`은 홈의 가입 CTA와 로그인 화면의 보조 링크로 제공한다.
- 적용 범위: 홈페이지 header, 사용자 로그인, 내정보와 회원가입 진입 흐름

### DEC-034: 회원가입 문구와 동의 항목 단순화

- 결정일: 2026-08-05
- 상태: `ACTIVE`
- 결정: 회원가입 화면의 장식용 영문·`hoiBot 회원가입` 문구를 제거하고 제목은 `회원가입`으로 표시한다.
- 동의: 이용약관만 필수 동의로 받고 개인정보 처리방침 동의 체크박스와 별도 동의 이력은 생성하지 않는다. 처리하는 최소 계정정보 안내 문구는 동의 항목이 아닌 정보성 안내로 유지한다.
- 적용 범위: 회원가입 UI·API, 동의 검증과 동의 이력

### DEC-035: HOIBOT 명칭의 의미와 사용 범위

- 결정일: 2026-08-05
- 상태: `SUPERSEDED`
- 대체 결정: `DEC-049` (2026-08-07)
- 결정: `HOIBOT`은 서비스·사이트·시스템 계정의 명칭이 아니라 KakaoTalk 등 외부 채널에 표시되는 봇의 프로필 이름이다.
- 표기 원칙: 사이트의 계정·서비스 명칭처럼 사용하지 않고, 사용자가 인증코드나 명령어를 입력할 봇 프로필을 지칭할 때만 사용한다.
- 적용 범위: 홈페이지 문구, 사용자 안내, KakaoTalk·Discord adapter와 운영 문서

### DEC-036: 사이트·브랜드 명칭

- 결정일: 2026-08-05
- 상태: `ACTIVE`
- 결정: 사이트명과 서비스 브랜드명은 `호이월드`로 사용한다.
- 구분: `호이월드`는 사이트·브랜드, `호월봇`은 외부 채널에 표시되는 봇 프로필 이름이다.
- 적용 범위: 사이트 header·문서 제목·footer·사용자 안내와 브랜드 표기

### DEC-037: 일반 사용자와 관리자 로그인 분리

- 결정일: 2026-08-05
- 상태: `ACTIVE`
- 결정: 일반 사용자는 사이트 우측 상단 로그인, 관리자는 공개 메뉴에 노출하지 않는 `/admin` 전용 화면과 관리자 인증 API를 사용한다.
- 로그인 후 이동: 관리자 로그인 성공 시 `/admin/console` 운영 콘솔로 이동한다.
- 적용 범위: 홈페이지 로그인, 관리자 로그인·세션·운영 화면

### DEC-038: 운영 권한·프리패스·테스트계정 분리와 REST API

- 결정일: 2026-08-05
- 상태: `ACTIVE`
- 권한 모델: 최고관리자와 매니저는 관리자 RBAC 역할로 관리하고 일반 사용자는 관리자 역할을 갖지 않는다. 매니저는 역할 기본 권한에 개인별 `allow`·`deny`를 적용하며 `deny`를 우선한다.
- 최고관리자 보호: 최고관리자는 운영자·권한을 관리할 수 있지만 마지막 활성 최고관리자는 정지·강등·삭제할 수 없다.
- 프리패스: 관리자 역할과 분리된 player 혜택으로 기간형과 영구형을 모두 지원하고 지급·회수 이력을 보존한다.
- 테스트계정: 운영 DB에 생성하지 않고 별도 테스트 환경에서만 생성·로그인할 수 있다.
- API: 사용자·관리자 인증 영역을 분리하고 `/api/v1/sessions`, `/api/v1/admin/sessions` 등 복수 명사·kebab-case REST resource URL을 사용한다. URL에 `login`, `approve`, `change` 같은 행위 동사를 사용하지 않는다.
- 변경 안전성: 운영 변경은 CSRF, `Idempotency-Key`, 필수 사유와 재확인, 감사 기록을 적용한다.
- 적용 범위: 사용자·관리자 인증, 관리자 인가, 운영 콘솔, 프리패스, 제재·탈퇴, 테스트계정과 REST API

### DEC-039: Iris 이름 캐시 미신뢰와 외부 identity 식별 기준

- 결정일: 2026-08-06
- 상태: `ACTIVE`
- 결정: Iris 최상위 `sender`와 `/data/local/tmp/names.db`에서 유래한 이름은 미신뢰 관측값으로 취급한다.
- 식별 기준: KakaoTalk 사용자는 문자열로 보존한 `chat_logs.user_id`를 외부 identity key로 사용하고 내부 `player_id`와 연결한다. Iris 캐시 이름은 회원 식별, 가입 인증, 권한, 소유권, 재화·아이템 변경에 사용하지 않는다.
- 이름 관리: Iris 캐시 이름은 출처와 미신뢰 상태를 붙인 append-only 관측 이력으로만 보관한다. 대표 이름은 provider 코드 인증, 관리자 승인 또는 직접 조회 가능한 KakaoTalk DB 근거로 검증된 경우에만 갱신한다.
- 충돌 처리: Iris 캐시 이름이 시스템 계정 이름 또는 다른 DB 관측값과 달라도 자동으로 닉네임 변경을 확정하거나 계정을 병합하지 않는다.
- 표시 규칙: 일반 이벤트 모니터에는 Iris 캐시 이름을 사람 이름으로 출력하지 않는다. 이벤트 소유자는 문자열 `user_id`로 표시하고, 현재 방과 해당 `user_id`를 함께 조회해 얻은 KakaoTalk DB 이름만 `확인된 이름`으로 표시한다. 조회 실패 시 이름 없음으로 표시한다.
- 적용 범위: Iris adapter, `/ping`, 회원가입·provider 연동, external identity, 닉네임 이력, 관리자 진단과 이벤트 모니터

### DEC-040: 승인된 오픈채팅방에서만 운영 명령 처리

- 결정일: 2026-08-06
- 상태: `SUPERSEDED` by `DEC-041`
- 결정: 운영 환경의 KakaoTalk 명령·회원 인증·게임 기능은 KakaoTalk DB에서 오픈채팅으로 확인되고 운영자가 활성화한 채널에서만 처리한다.
- 현재 허용 유형: 실제 확인된 오픈 멀티채팅 유형 `OM`만 허용한다. `DirectChat`, `MultiChat`, `PlusChat`과 확인되지 않은 유형은 기본 차단하며, 향후 오픈 1:1 유형은 실제 데이터 검증과 별도 결정 전까지 허용하지 않는다.
- 판정 기준: Iris의 `room` 이름이나 캐시 문자열을 사용하지 않고 `chat_id`로 조회한 `db1.chat_rooms.type`과 hoiBot 채널 활성 상태를 함께 확인한다. 유형을 조회할 수 없으면 안전하게 차단한다.
- 차단 동작: 허용되지 않은 방 이벤트는 전송 계층에서 재시도 방지를 위해 수신할 수 있으나 명령 응답, 회원·identity 연결, 게임 도메인 변경, 활동 집계, 사건·이름 관측과 outbox 생성을 수행하지 않는다.
- 개발 예외: 비운영 환경에서 명시적으로 등록한 진단방은 `/ping`, `/info`, 이벤트 유형 검증만 허용한다. 가입·인증·재화·아이템·관리 명령 등 상태 변경 기능은 실행하지 않는다.

### DEC-041: 오픈채팅 단체방과 오픈채팅 1:1 허용

- 결정일: 2026-08-06
- 상태: `ACTIVE`
- 결정: 운영 환경에서 허용할 KakaoTalk 채널 범위는 오픈채팅 단체방과 오픈채팅 1:1이다. 두 유형 모두 운영자가 hoiBot 사용 채널로 활성화해야 명령·회원 인증·게임 기능을 처리한다.
- 현재 검증 상태: 실제 DB에서 `db1.chat_rooms.type=OM`, `link_id` 존재, 활성·미만료 `db2.open_link` 연결이 오픈채팅 단체방으로 확인됐다. 오픈채팅 1:1의 물리 `chat_rooms.type` 값은 아직 실제 데이터로 확인되지 않았다.
- 판정 기준: `chat_id`로 조회한 `chat_rooms` 행, `link_id`, 대응하는 활성·미만료 `open_link` 행, 검증된 오픈채팅 방 유형 매핑을 함께 사용한다. 방 이름과 Iris 캐시 문자열은 인가에 사용하지 않는다.
- 안전 규칙: 오픈채팅 1:1을 허용하기 위해 일반 `DirectChat`을 대신 허용하지 않는다. 실제 오픈채팅 1:1 샘플을 관측해 물리 유형과 오픈링크 관계를 검증한 뒤 매핑에 등록한다. 미확인 유형과 조회 실패는 기본 차단한다.
- 차단 대상: 일반 `DirectChat`, 일반 `MultiChat`, `PlusChat` 및 오픈링크가 검증되지 않은 채널은 운영 기능을 처리하지 않는다.
- 개발 예외: 비운영 환경에서 명시적으로 등록한 진단방은 `/ping`, `/info`, 이벤트 유형 검증만 허용하며 상태 변경 기능은 실행하지 않는다.
- 적용 범위: Iris adapter, 채널 정책, 명령 dispatch, 회원가입·provider 인증, 이벤트 모니터링과 개인정보 최소화

### DEC-042: 삭제·방장 가리기 이벤트의 원본 메시지 표시

- 결정일: 2026-08-06
- 상태: `SUPERSEDED`
- 대체 결정: `DEC-046` (2026-08-06)
- 결정: 비운영 Iris 이벤트 모니터에서 메시지 삭제 또는 방장 가리기 이벤트가 발생하면, 동일 `chat_id`와 대상 `logId`로 KakaoTalk `chat_logs` 원본 행을 즉시 조회하여 복호화된 원본 메시지를 모니터링 알림에 함께 표시한다.
- 조회 규칙: 64비트 대상 ID를 문자열로 유지하고 기존 `(chat_id,id)` 인덱스를 사용한다. 조회 실패와 대상 행 없음은 원문 대신 상태를 표시하며 이벤트 알림 자체는 계속 전송한다.
- 최소화 규칙: 복원한 원문은 hoiBot MariaDB·감사 로그·문서에 저장하지 않고 해당 모니터링 알림에만 사용한다. 출력은 최대 1,000자로 제한하며 이미지·미디어 파일 자체는 전송하지 않는다.
- 적용 범위: 비운영 Iris 이벤트 모니터의 `message.deleted`, `message.hidden_by_host`

### DEC-043: Iris 공식 계약과 실측 진단 절차의 스킬화

- 결정일: 2026-08-06
- 상태: `ACTIVE`
- 결정: redroid/Iris 이벤트, KakaoTalk `chat_logs`, `/query`, `/decrypt`, 삭제·가리기 상관관계, MariaDB 사건·outbox 문제는 저장소 전용 `hoibot-iris-diagnostics` 스킬을 먼저 사용해 계층별로 진단한다.
- 정보 분리: 공식 KBotDocs API 계약과 현재 환경 실측 예외는 `../.codex/skills/hoibot-iris-diagnostics/references/iris-contract.md`에서 출처와 성격을 구분해 관리한다. 실행 절차는 `../.codex/skills/hoibot-iris-diagnostics/SKILL.md`, 반복되는 읽기 전용 조회는 같은 스킬의 `scripts/inspect-event.mjs`에서 관리한다.
- 세션 유지: 저장소 사본을 기준으로 관리하고 사용자 로컬 Codex 스킬 디렉터리에도 동기화하여 다른 세션에서 자동 발견되게 한다.
- 안전 규칙: 구성값·토큰·AOT·개인정보 원문을 문서나 Git에 저장하지 않는다. 원문은 명시적 요청이 있을 때만 진단 출력하고 기본 스크립트 출력에서는 숨긴다.
- 적용 범위: Iris adapter, 이벤트 모니터, DB 진단, 복호화, redroid 연결 검증 및 관련 장애 대응

### DEC-044: KakaoTalk 이벤트의 2단계 채널 필터

- 결정일: 2026-08-06
- 상태: `SUPERSEDED`
- 대체 결정: `DEC-048` (2026-08-07)
- 1차 필터: KakaoTalk DB 근거로 오픈채팅 단체방 또는 오픈채팅 1:1로 확인된 이벤트만 허용 후보로 분류한다. 일반 `DirectChat`, `MultiChat`, `PlusChat`과 미확인 유형은 제외한다.
- 2차 필터: 허용 후보 중 운영자가 hoiBot 채널로 지정·활성화한 오픈채팅방에서만 명령, 회원 인증, 게임 처리, 활동 집계와 사건 모니터링을 수행한다.
- 수신 경계: Iris가 다른 방의 이벤트를 HTTP로 전달할 수는 있으나 서버는 재시도 방지를 위한 수신 확인 후 두 필터를 통과하지 못한 이벤트를 업무 처리 전에 폐기한다.
- 판정 기준: 방 이름이 아니라 문자열 `chat_id`, `chat_rooms`, `link_id`, 활성·미만료 `open_link`, 검증된 물리 방 유형 매핑과 hoiBot 채널 활성 상태를 사용한다.
- 적용 범위: Iris adapter, 명령 dispatch, 회원 인증, 활동·사건 모니터링, 개인정보 최소화

### DEC-045: 삭제·가리기 원문 조회 권한

- 결정일: 2026-08-06
- 상태: `ACTIVE`
- 결정: 관리자 홈페이지에서 삭제·방장 가리기 사건의 복원 원문을 확인할 수 있는 역할은 `super_admin`과 `manager`다. 일반 사용자는 확인할 수 없다.
- 권한 모델: 사건 메타데이터 조회와 원문 조회 권한을 분리하고, 원문 조회 권한은 두 관리자 역할의 기본 권한으로 둔다. 기존 RBAC의 개인별 `deny`는 매니저에게 계속 우선 적용한다.
- 현재 보존 방식: `DEC-042`에 따라 복원 원문은 hoiBot MariaDB에 저장하지 않는다. 홈페이지 조회를 구현할 때는 사건의 `chat_id`와 대상 log ID로 redroid KakaoTalk DB를 실시간 조회하고, 조회 불가 시 원문을 보존된 것처럼 표시하지 않는다.
- 감사: 누가 어떤 사건의 원문 조회를 요청했는지는 감사하되, 감사 기록에 원문 자체를 복사하지 않는다.
- 적용 범위: 관리자 RBAC, 감지 사건 API·화면, Iris `/query`·`/decrypt`, 감사 기록

### DEC-046: 삭제 사건번호와 KakaoTalk `/열람` 흐름

- 결정일: 2026-08-06
- 상태: `ACTIVE`
- 결정: 삭제 감지 알림에는 원문을 즉시 노출하지 않고 열람 번호, 실제 확인된 사용자 이름과 실제 확인된 방 이름을 표시한다. 사용자는 정확한 `/열람 #번호` 명령으로 원문을 조회한다.
- 출력 형식: 다른 봇의 기능 흐름만 가져온다. 카카오톡 출력에는 `Iris`, `사건`, `용의자` 표현을 사용하지 않고 `메시지 삭제 감지`, `방`, `사용자`, `열람 번호`, `/열람`만 표시한다.
- 사건번호: hoiBot `moderation_incidents.id`를 외부 표시용 `#번호`로 사용한다. 사건에는 방 ID, 대상 메시지 ID, 사건 유형과 시각만 기록하고 복원 원문은 저장하지 않는다.
- 이름 근거: 사용자는 현재 방과 `user_id`로 조회한 `open_chat_member.nickname`, 방은 `open_link.name`을 우선하고 `chat_rooms.meta`의 방 제목을 보조로 사용한다. Iris `sender`와 `room` 캐시는 실제 이름 출력에 사용하지 않는다.
- 원문 조회: `/열람` 시 사건 메타데이터로 redroid KakaoTalk DB를 실시간 조회·복호화한다. 원문을 찾지 못하거나 redroid가 응답하지 않으면 조회 불가 상태를 명시한다.
- 진단방: 비운영 모니터링방의 삭제·가리기 테스트도 사건번호를 발급한다. `event_inbox`, 정규화 사건, 사건 상관 메타데이터와 알림 전송에 필요한 operation·outbox·delivery만 기록하며 identity·활동·회원·게임 데이터는 변경하지 않는다.
- 적용 범위: 삭제·방장 가리기 사건 알림, KakaoTalk 사건 열람 명령, 이벤트 모니터

### DEC-047: 방별 사용자 입장·퇴장 기록

- 결정일: 2026-08-07
- 상태: `ACTIVE`
- 결정: 오픈채팅방에서 사용자별 입장·퇴장 이력을 방 단위로 append-only 기록하고, 감지 알림에 해당 방의 방문 요약을 표시한다.
- 사용자 식별: `NEWMEM`의 단일 `members[0]`, `DELMEM`의 `member`에 포함된 `userId`를 문자열 외부 identity key로 사용하고 `nickName`을 해당 시점의 검증된 방별 이름으로 기록한다. Iris 이름 캐시는 사용하지 않는다.
- 표시 정보: 실제 방 이름, 실제 사용자명, 내부 계정 연동 여부, 방문 횟수, 첫 입장, 이전 퇴장, 현재 입·퇴장 시각, 누적 채팅 수, 삭제 감지 수, 이전 닉네임과 최근 출입 이력을 hoiBot 형식으로 표시한다.
- 개인정보 최소화: 첫·마지막 채팅 원문은 보관하거나 출력하지 않고 집계 건수만 사용한다. 전화번호, 프로필 이미지와 외부 URL도 저장하지 않는다.
- 미확정 구분: `DELMEM`에서 자진 퇴장과 강제 퇴장을 구분할 실측 근거가 확보되기 전에는 모두 `퇴장`으로 기록한다. 경고 횟수도 별도 경고 정책 구현 전에는 표시하지 않는다.
- 진단방: 비운영 모니터링방에서도 입장·퇴장 테스트에 필요한 channel, external identity, 이름 관측, membership 이력과 알림 outbox만 기록하며 회원·게임 데이터는 변경하지 않는다.
- 적용 범위: Iris 정규화, channel membership, 이벤트 모니터, 향후 관리자 출입 이력 화면

### DEC-048: 단계형 오픈채팅 관찰과 관리자 모니터링 콘솔

- 결정일: 2026-08-07
- 상태: `ACTIVE`
- 1차 검증: KakaoTalk DB에서 활성·미만료 오픈채팅으로 확인된 모든 방의 이벤트를 관찰하고 최소 메타데이터를 적재한다. 일반 채팅방과 미확인 유형은 계속 차단한다.
- 명령 경계: 모든 오픈방 관찰은 명령 실행 권한을 의미하지 않는다. 명령, 회원 인증과 게임 기능은 운영자가 지정한 오픈채팅방에서만 실행한다.
- 2차 운영: 검증 종료 후 환경 설정을 `designated_only`로 바꿔 지정된 활성 오픈채팅방만 관찰·처리한다.
- 관리자 화면: `super_admin`과 `manager`는 `monitoring.read` 권한으로 일반 텍스트를 제외한 이벤트, 삭제·가리기 사건, 입장·퇴장 이력을 조회한다.
- 적재 범위: 이벤트 분류, type/origin/direction, 문자열 외부 ID, 대상 이벤트 ID, 시각, 실제 DB에서 확인된 방 이름·사용자 이름 이력, 활동 건수만 저장한다.
- 개인정보 최소화: 일반 메시지 본문, 삭제 원문, 이미지·미디어 URL, 프로필 이미지는 저장하지 않는다. 삭제 원문은 기존 `/열람`과 향후 권한 API에서 redroid DB를 실시간 조회한다.
- 적용 범위: Iris 채널 정책, event inbox/normalization, 방·사용자 이력, 관리자 REST API와 콘솔

### DEC-049: 외부 봇 프로필 이름

- 결정일: 2026-08-07
- 상태: `ACTIVE`
- 결정: KakaoTalk 등 외부 채널에 표시되는 봇 프로필 이름은 `호월봇`이다.
- 구분: 사이트·서비스 브랜드는 `호이월드`, 외부 봇 프로필은 `호월봇`, 내부 프로젝트·서버 식별자는 기존 `hoiBot`을 유지한다.
- 변경: 기존 `HOIBOT` 프로필 명칭 결정을 대체하며 회원가입·연동 안내 등 사용자 화면은 `호월봇`으로 표시한다.
- 적용 범위: 공개 웹 화면, 가입·연동 안내, 카카오톡 운영 안내

### DEC-050: 이벤트 감지 알림 표시 형식

- 결정일: 2026-08-07
- 상태: `ACTIVE`
- 결정: 일반 이벤트 감지 알림은 `호월봇 이벤트 감지 -> 이벤트 종류 -> 실제 확인된 방 -> 실제 확인된 사용자 -> 방향` 순서로 표시한다.
- 표시 제한: KakaoTalk 사용자·채팅방·이벤트 ID, Iris 명칭, type/origin 등 운영자가 알림에서 몰라도 되는 진단 필드는 표시하지 않는다. 후보·미확정 이벤트는 내부 상태 코드 대신 `추가 확인 필요`로 안내한다.
- 신뢰 기준: 방과 사용자 이름은 KakaoTalk DB에서 확인된 값만 표시하고, Iris 캐시 이름은 대체값으로 노출하지 않는다.
- 적용 범위: 이미지, 동영상, 이모티콘, 멘션, 답글, 검색·링크 카드와 기타 일반 이벤트 모니터링 알림

### DEC-051: 홈페이지 실제 데이터 표시 원칙

- 결정일: 2026-08-07
- 상태: `ACTIVE`
- 결정: 호이월드 홈페이지에 표시하는 수치와 캐릭터 정보는 실제 hoiBot API와 MariaDB 조회 결과만 사용한다.
- 미구현 기능: 동작하는 서버 API와 데이터가 없는 메뉴·버튼·통계·상세 화면은 사용자 화면에 표시하지 않는다. 구현과 검증이 끝난 뒤에만 노출한다.
- 공개 범위: 공개 홈은 개인 식별정보가 없는 집계와 서비스 상태만 표시한다. 로그인 사용자의 내정보는 본인 세션으로 조회한 `player_id`의 프로필만 표시한다.
- 적용 범위: 공개 홈, 로그인 사용자 내정보, 관리자 콘솔, 향후 랭킹·거래소·길드·도감 화면

### DEC-052: 기능 필요 이벤트 콘텐츠의 제한 보관

- 결정일: 2026-08-07
- 상태: `ACTIVE`
- 결정: 홈페이지 모니터링 기능에 실제 내용 표시가 필요한 경우 메시지 본문, 이미지와 미디어 URL을 제한적으로 저장할 수 있다.
- 대상: 운영자가 지정한 활성 오픈채팅방에서 확정 분류된 답글, 단일·다중 이미지와 움직이는 이모티콘만 보관한다. 일반 텍스트, 후보·미분류 이벤트와 1차 전체 오픈방 관찰 데이터는 저장하지 않는다.
- 보관 방식: 답글 본문과 필요한 미디어 메타데이터는 MariaDB, 미디어 파일은 외부 공개 경로가 아닌 서버 전용 저장소에 둔다. 신뢰된 Kakao HTTPS URL만 다운로드하며 URL과 본문은 7일 후 자동 삭제한다.
- 보안·백업: 개발 환경은 DB 권한과 비공개 파일 경로로 접근을 제한한다. 운영 전환 전에는 HTTPS와 호스트의 암호화된 디스크·볼륨을 필수 검증하고, 장기 백업에서 이 임시 콘텐츠 저장소를 제외하거나 동일한 7일 이내 만료를 적용한다.
- 권한과 감사: `super_admin`과 `manager`의 `event.content.read` 권한으로만 내용과 파일을 열람한다. 매니저 개인 `deny`가 우선하며 내용·미디어 열람은 별도 감사 기록을 남긴다.
- 기존 정책과 관계: `DEC-048`의 개인정보 최소화 원칙은 유지하되, 위 기능 필요 콘텐츠에 한해서만 명시적 예외를 둔다. 삭제·방장 가리기 원문은 계속 저장하지 않고 `DEC-045`와 `DEC-046`의 실시간 조회 방식을 유지한다.
- 적용 범위: Iris event adapter, 콘텐츠 보관·정리 worker, 관리자 REST API와 모니터링 화면

### DEC-053: 1차 콘텐츠 보관 대상은 모든 오픈채팅방

- 결정일: 2026-08-07
- 상태: `ACTIVE`
- 결정: 1차 검증 기간에는 기능 필요 이벤트 콘텐츠의 방 범위를 모든 오픈채팅방으로 운영한다.
- 판정 기준: `DEC-041`과 `DEC-048`에 따라 KakaoTalk DB에서 활성·미만료 오픈채팅 단체방 또는 검증된 오픈채팅 1:1로 확인된 방만 포함한다. 일반 `DirectChat`, `MultiChat`, `PlusChat`과 미확인 방은 포함하지 않는다.
- 콘텐츠 범위: `DEC-052`의 확정 답글·단일·다중 이미지·움직이는 이모티콘만 7일 보관하며 일반 텍스트·후보 이벤트·삭제 원문은 계속 제외한다.
- 전환: 1차 검증 후 `RETAINED_EVENT_CONTENT_SCOPE=designated_only`로 변경하여 별도 지정한 오픈채팅방만 보관할 수 있다. 콘텐츠 방 지정은 게임 명령 허용 방 지정과 별도 설정으로 관리한다.
- 적용 범위: Iris 채널 정책, 이벤트 콘텐츠 보관 설정, 관리자 모니터링 화면

### DEC-054: 관리자 이벤트 모니터링 분류

- 결정일: 2026-08-07
- 상태: `SUPERSEDED`
- 대체 결정: `DEC-055` (2026-08-07)
- 결정: 관리자 `로그·모니터링`에서 일반 이벤트를 `미디어`, `답글`, `이벤트 감지`로 분리해 조회한다.
- 미디어: 단일·다중 이미지와 동영상만 포함한다. 확정 이미지는 `DEC-052`와 `DEC-053`의 제한 보관 정책에 따라 실제 콘텐츠를 표시할 수 있고, 동영상은 현재 감지 메타데이터만 표시한다. 이모지는 미디어가 아닌 `이벤트 감지`로 분류한다.
- 답글: 일반 답글, 스레드 답글과 확정된 답글 이벤트를 별도 목록으로 표시한다.
- 이벤트 감지: 미디어·답글·삭제·가리기·입장·퇴장을 제외한 멘션, 검색·링크 카드와 기타 확정 이벤트만 표시한다.
- 데이터 분류: `normalized_provider_events.monitoring_group`에 `text`, `media`, `reply`, `moderation`, `membership`, `event` 중 하나를 저장한다. 관리자 화면은 이벤트 코드 문자열을 다시 추측하지 않고 이 영속 분류를 조회한다.
- 표시 원칙: 각 탭은 MariaDB 전체 건수와 최신 목록을 실제 조회하며, 일반 텍스트와 사용자에게 불필요한 내부 기술 필드는 표시하지 않는다.
- 적용 범위: 관리자 모니터링 REST API, 종합 현황과 하위 메뉴

### DEC-055: 관리자 모니터링 최종 메뉴와 데이터 분류

- 결정일: 2026-08-07
- 상태: `ACTIVE`
- 관리자 메뉴: `삭제·수정`, `들낙`, `입장·퇴장`, `미디어 (이미지·영상)`과 운영 전송 실패 확인만 제공한다.
- 삭제·수정: 메시지 삭제, 방장 가리기와 메시지 수정을 하나의 운영 목록으로 표시한다.
- 미디어: 단일·다중 이미지와 영상만 포함한다. 이모지·이모티콘과 답글은 관리자 미디어·독립 메뉴에 표시하지 않고 내부 기타 이벤트로만 분류한다.
- 검색·링크: 현재 저장 데이터로 카드 내용을 확인할 수 없으므로 관리자 메뉴를 제공하지 않는다. 실제 내용 열람 근거가 구현·검증되기 전에는 다시 노출하지 않는다.
- 영속 분류: `normalized_provider_events.monitoring_group`은 `text`, `media`, `moderation`, `membership`, `event`만 사용한다. 화면은 이 분류와 사건·출입 전용 테이블을 조회한다.
- 적용 범위: 이벤트 정규화, MariaDB 기존 데이터 백필, 관리자 REST API와 모니터링 화면

### DEC-056: 들낙 판정 기준

- 결정일: 2026-08-07
- 상태: `ACTIVE`
- 결정: 같은 채팅방에서 동일 사용자가 입장 2회 이상 또는 퇴장 2회 이상 기록되면 들낙 대상으로 표시한다.
- 기간: 24시간 등 별도 시간 제한을 적용하지 않고 해당 방에 저장된 전체 출입 이력을 기준으로 집계한다.
- 표시: 입장 횟수, 퇴장 횟수, 둘 중 큰 반복 횟수와 가장 최근 출입 시각을 표시한다.
- 상세: 들낙 항목의 `상세 보기`를 누르면 해당 방·사용자의 전체 입장·퇴장 이력을 팝업으로 표시한다.
- 적용 범위: 관리자 들낙 조회 API, 종합 현황과 들낙 목록

### DEC-057: 운영자와 관리 채팅방의 다대다 배정

- 결정일: 2026-08-07
- 상태: `ACTIVE`
- 결정: 운영자와 관리 채팅방은 다대다 관계로 배정한다. 한 운영자는 여러 방을 관리할 수 있고, 한 방도 여러 운영자가 공동 관리할 수 있다.
- 예시: 운영자 A는 A방만, 운영자 B는 A방과 B방을 동시에 관리할 수 있다.
- 권한 판정: 기능 권한과 담당 방 배정을 함께 만족해야 해당 방의 모니터링 데이터를 조회·관리할 수 있다. 프론트엔드 필터가 아니라 서버 API와 SQL에서 방 범위를 강제한다.
- 적용 범위: 운영자-채널 배정 테이블, 관리자 세션 인가, 삭제·수정·들낙·입퇴장·미디어 조회와 원문·파일 열람

### DEC-058: 삭제·가리기와 메시지 수정 모니터링 분리

- 결정일: 2026-08-07
- 상태: `ACTIVE`
- 결정: 관리자 `로그·모니터링`에서 삭제·가리기와 메시지 수정을 별도 메뉴 및 실제 API 필터로 분리한다.
- 삭제·가리기: 메시지 삭제와 방장 가리기만 표시하고, 원문은 권한 확인 후 redroid KakaoTalk DB에서 실시간 조회한다.
- 메시지 수정: 수정 사건만 표시한다. 원문 보기는 현재 대상 행의 수정 후 본문과 `modifyLog`의 가장 최근 직전 수정 전 본문을 각각 조회·복호화해 함께 보여 준다.
- 보관 정책: 수정 전·후 본문은 MariaDB에 새로 저장하지 않으며, 각 열람의 감사 사실만 남긴다. 다중 수정 이력은 별도 검증 전까지 보장하지 않는다.
- 적용 범위: 관리자 moderation incident REST API, redroid 실시간 조회 adapter, 관리자 모니터링 화면

## 대체 관계 요약

- `DEC-006`의 점진적 운영 전환은 `DEC-021`의 전체 검증 후 일괄 전환으로 대체됐다.
- `DEC-012`와 `DEC-013`은 `DEC-014`로, `DEC-014`는 이미지 자동 전송을 끈 `DEC-015`로 대체됐다.
- `DEC-040`의 오픈채팅 단체방 한정은 오픈채팅 1:1까지 허용하는 `DEC-041`로 대체됐다.
- `DEC-042`의 감지 알림 원문 즉시 표시는 사건번호와 `/열람` 흐름을 사용하는 `DEC-046`으로 대체됐다.
- `DEC-044`의 지정방 외 이벤트 즉시 폐기는 1차 전체 활성 오픈방 관찰 후 2차 지정방 제한을 정의한 `DEC-048`로 대체됐다.
- `DEC-035`의 외부 봇 프로필명 `HOIBOT`은 `DEC-049`의 `호월봇`으로 대체됐다.
- `DEC-054`의 미디어·답글·기타 이벤트 메뉴는 `DEC-055`의 운영 중심 최종 메뉴로 대체됐다.
- `DEC-031`의 개인정보 처리방침 동의와 CAPTCHA 부분은 각각 `DEC-034`, `DEC-032`로 대체됐다.
